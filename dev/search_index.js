var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/types/#Types-API-Reference","page":"Types","text":"This page documents the core types in ERGMUserterms.jl.","title":"Types API Reference"},{"category":"section","location":"api/types/#Package","page":"Types","text":"","title":"Package"},{"category":"section","location":"api/types/#Term-Base-Types","page":"Types","text":"","title":"Term Base Types"},{"category":"section","location":"api/types/#Example-Terms","page":"Types","text":"","title":"Example Terms"},{"category":"section","location":"api/types/#Term-Interface","page":"Types","text":"All terms (both user-defined and built-in) must implement these methods. The generics are owned by ERGM.jl and re-exported by ERGMUserterms.jl.","title":"Term Interface"},{"category":"module","location":"api/types/#ERGMUserterms","page":"Types","text":"ERGMUserterms.jl - Custom ERGM Term Development\n\nProvides templates, utilities, and validation tools for developing custom ERGM terms. Includes example terms and a comprehensive testing framework.\n\nPort of the R ergm.userterms package from the StatNet collection. What is ported is the role of that package — a validated, copyable starting point for third-party terms — not its contents: R's C-level changestats.users template and its mindegree example term are not ported (the Julia counterpart of the C template is examples/MyTermPackage/; a Julia term is plain Julia, so there is no C skeleton to fill in).\n\nThe interface generics a term extends — name, compute, change_stat — are ERGM.jl's (compute/name are really Networks.jl's shared statistic protocol); this package imports and re-exports them, so import ERGM: … and import ERGMUserterms: … name the same functions. Edge endpoints src/dst are Graphs.src/Graphs.dst, which neither Networks.jl nor ERGM.jl re-exports: a term iterating edges(net) needs using Graphs: src, dst.\n\n\n\n\n\n","title":"ERGMUserterms"},{"category":"type","location":"api/types/#ERGMUserterms.AbstractUserTerm","page":"Types","text":"AbstractUserTerm <: AbstractERGMTerm\n\nBase type for user-defined ERGM terms. A subtype is a full ERGM.jl term — it can go into an ERGMFormula, be fitted with fit_ergm/ergm and simulated from — once it has name, compute and change_stat methods (the add-direction convention) and declares the traits that differ from the defaults (ERGM.required_vertex_attributes, ERGM.requires_directed, ERGM.is_dyad_dependent, Networks.supports_missing, …). The fallback ERGM.is_dyad_dependent(::AbstractUserTerm) is true, the conservative answer; ERGM's own NodalTerm/DyadicTerm supertypes imply false, so subtype those only for covariate-only terms.\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nimport ERGM: name, compute, change_stat\nstruct EdgeCount <: AbstractUserTerm end\nname(::EdgeCount) = \"edgecount\"\ncompute(::EdgeCount, net) = Float64(ne(net))\nchange_stat(::EdgeCount, net, i::Int, j::Int) = 1.0\nERGM.is_dyad_dependent(::EdgeCount) = false          # covariate-only: exact MPLE\nEdgeCount() isa AbstractERGMTerm                     # true — a first-class ERGM term\nnet = network(6; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 2, 3)\nvalidate_term(EdgeCount(), net; verbose=false, rng=Xoshiro(1))   # true\nERGMModel(ERGMFormula([EdgeCount()]), net) isa ERGMModel         # true\n\n\n\n\n\n","title":"ERGMUserterms.AbstractUserTerm"},{"category":"type","location":"api/types/#ERGMUserterms.ExampleTerm","page":"Types","text":"ExampleTerm <: AbstractUserTerm\n\nExample custom term that counts edges weighted by vertex ID sum. Use this as a template for creating your own terms.\n\nAll change_stat methods must return the add-direction change statistic g(y⁺ij) − g(y⁻ij) — the statistic with edge (i,j) present minus with it absent — independent of whether the edge currently exists. ERGM.jl's MPLE design matrix and MH sampler both require this convention.\n\nIdentical to statnet's nodecov(\"id\") with id = vertex index; the golden fixture test/fixtures/userterms_examples.toml pins it against ergm on every dyad. Covariate-only, so it declares ERGM.is_dyad_dependent = false.\n\nExample\n\nusing ERGM, ERGMUserterms, Networks\nnet = network(5; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 3, 4)\ncompute(ExampleTerm(), net)              # 10.0 — (1 + 2) + (3 + 4)\nchange_stat(ExampleTerm(), net, 2, 3)    # 5.0\nchange_stat(ExampleTerm(), net, 1, 2)    # 3.0 — edge present: same add-direction value\nname(ExampleTerm())                      # \"example\"\n\n\n\n\n\n","title":"ERGMUserterms.ExampleTerm"},{"category":"type","location":"api/types/#ERGMUserterms.TemplateTerm","page":"Types","text":"TemplateTerm{T} <: AbstractUserTerm\n\nA parameterized template term — param times the number of edges. Copy and modify this for your own terms. TemplateTerm(1.0) is statnet's edges (pinned by the golden fixture).\n\nFields\n\nparam::T: A parameter value\nattr::Symbol: An attribute name (optional; unused by the template's statistic — a placeholder for the attribute your term reads)\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nnet = network(5; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 2, 3)\nt = TemplateTerm(2.5)\nname(t)                                  # \"template.2.5\"\ncompute(t, net)                          # 5.0 — 2 edges × 2.5\nchange_stat(t, net, 3, 4)                # 2.5\nTemplateTerm(3; attr=:x).attr            # :x\nvalidate_term(t, net; verbose=false, rng=Xoshiro(1))   # true\n\n\n\n\n\n","title":"ERGMUserterms.TemplateTerm"},{"category":"type","location":"api/types/#ERGMUserterms.WeightedEdges","page":"Types","text":"WeightedEdges <: AbstractUserTerm\n\nSum of edge weights, with default (1.0) for edges lacking the weight attribute. Demonstrates accessing edge attributes.\n\ncompute sums over the network's edges, reading each edge's weight with the per-edge getter get_edge_attribute(net, attr, i, j) (which returns nothing for an edge — or a network — without the attribute) and falling back to default; that keeps it consistent with change_stat when edges are added without weights (e.g. by the MH sampler). For the same reason the term declares no required edge attribute: it is well defined on a network that has never heard of attr. On an undirected network the getter reads the canonical (min, max) key itself. Identical to statnet's edgecov(W) with W the stored weights and default elsewhere.\n\nThe read is asserted ::Float64, so compute/change_stat infer Float64 and allocate nothing per call (the attribute store is untyped, Dict{…,Any}; without the assertion the term would infer Any and box on every MPLE row and MH step of any model containing it).\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nnet = network(4; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 2, 3)\nset_edge_attribute!(net, :weight, 1, 2, 3.0)\nt = WeightedEdges()                      # attr = :weight, default = 1.0\ncompute(t, net)                          # 4.0 — 3.0 + the default 1.0\nchange_stat(t, net, 1, 2)                # 3.0\nchange_stat(t, net, 3, 4)                # 1.0 — a fresh edge gets the default\nERGM.required_edge_attributes(t)         # () — deliberately undeclared\nvalidate_term(t, net; verbose=false, rng=Xoshiro(1))   # true\n\n\n\n\n\n","title":"ERGMUserterms.WeightedEdges"},{"category":"type","location":"api/types/#ERGMUserterms.DyadCovTerm","page":"Types","text":"DyadCovTerm <: AbstractUserTerm\n\nDyadic covariate term: sum over edges of covariate[i, j]. Demonstrates using a matrix covariate. Identical to statnet's edgecov(covariate); on an undirected network the entry is read at the canonical (min, max) position, so pass a symmetric matrix (or accept that only its upper triangle counts). Entries outside the matrix contribute 0.\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\ndistance = [0.0 1.0 2.0 3.0\n            1.0 0.0 1.5 2.5\n            2.0 1.5 0.0 1.0\n            3.0 2.5 1.0 0.0]\nnet = network(4; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 3, 4)\nt = DyadCovTerm(distance)\ncompute(t, net)                          # 2.0 — distance[1,2] + distance[3,4]\nchange_stat(t, net, 1, 3)                # 2.0\nvalidate_term(t, net; verbose=false, rng=Xoshiro(1))   # true\n\n\n\n\n\n","title":"ERGMUserterms.DyadCovTerm"},{"category":"type","location":"api/types/#ERGMUserterms.InteractionTerm","page":"Types","text":"InteractionTerm <: AbstractUserTerm\n\nInteraction between two vertex attributes: the sum over edges (i, j) of a_i b_j + a_j b_i. Identical to statnet's edgecov(M) with M = a bᵀ + b aᵀ. It declares both attributes (ERGM.required_vertex_attributes), so ERGMModel construction refuses a network on which either is absent or set on only some vertices — the statnet NA rule — instead of fitting a silent all-zero column. Values are read per vertex with get_vertex_attribute(net, attr, v) and asserted ::Float64, so compute/change_stat infer Float64 and allocate nothing.\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nnet = network(4; directed=true)\nadd_edge!(net, 1, 2)\nset_vertex_attribute!(net, :age, Dict(1 => 25.0, 2 => 30.0, 3 => 40.0, 4 => 50.0))\nset_vertex_attribute!(net, :income, Dict(1 => 5.0, 2 => 6.0, 3 => 7.0, 4 => 8.0))\nt = InteractionTerm(:age, :income)\ncompute(t, net)                          # 300.0 — 25·6 + 30·5\nERGM.required_vertex_attributes(t)       # (:age, :income)\nvalidate_term(t, net; verbose=false, rng=Xoshiro(1))   # true\ntry; ERGMModel(ERGMFormula([InteractionTerm(:age, :height)]), net); catch e; e isa ArgumentError; end   # true\n\n\n\n\n\n","title":"ERGMUserterms.InteractionTerm"},{"category":"function","location":"api/types/#Networks.name","page":"Types","text":"name(term::AbstractERGMTerm) -> String\n\nReturn the term's descriptive, lowercase name, including any parameters (e.g. \"template.2.0\", \"weightedges.weight\").\n\nThis generic is owned by ERGM.jl (it is Networks.name, the ecosystem's shared statistic protocol) and re-exported here: ERGMUserterms.name === ERGM.name === Networks.name. Add a method for your term type after importing it by name — import ERGM: name or import ERGMUserterms: name, the same function; a bare using followed by name(::MyTerm) = … defines a local name that ERGM.jl never calls. Without a method, ERGM.jl falls back to string(typeof(term)).\n\nUse R ergm's label where one exists (\"gwdeg.fixed.0.5\", \"nodematch.group\") so coefficient tables line up with statnet output. ERGM.jl also has a two-argument name(term, net) for labels that depend on the network's direction (\"gwesp.OTP.fixed.0.5\" on a digraph); it falls back to name(term), so a user term needs only the one-argument method.\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nimport ERGM: name, compute, change_stat     # extend the shared generics\nstruct OneEdge <: AbstractUserTerm end\nname(::OneEdge) = \"oneedge\"\ncompute(::OneEdge, net) = Float64(ne(net))\nchange_stat(::OneEdge, net, i::Int, j::Int) = 1.0\nname(OneEdge())                                   # \"oneedge\"\nname(OneEdge()) == ERGM.name(OneEdge())           # true — one generic, not a local copy\nvalidate_term(OneEdge(), network(4; directed=true); verbose=false, rng=Xoshiro(1))   # true\n\n\n\n\n\n","title":"Networks.name"},{"category":"function","location":"api/types/#Networks.compute","page":"Types","text":"compute(term::AbstractERGMTerm, net) -> Float64\n\nCompute the term's full network statistic g(y) on net.\n\nThis generic is owned by ERGM.jl (it is Networks.compute, the ecosystem's shared statistic protocol) and re-exported here: ERGMUserterms.compute === ERGM.compute === Networks.compute. Add a method for your term type after import ERGM: compute (or import ERGMUserterms: compute — the same function). It must be deterministic, must not modify the network, and should handle empty and complete networks.\n\nEdge endpoints are Graphs.src/Graphs.dst, which Networks.jl does not re-export — a compute that iterates edges(net) needs using Graphs: src, dst. A vertex attribute the term declares (ERGM.required_vertex_attributes) is guaranteed complete inside an ERGMModel (ERGM refuses a partial one, as statnet refuses NA), so get(attrs, v, default) fallbacks only ever fire on raw compute calls.\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nusing Graphs: src, dst\nimport ERGM: name, compute, change_stat\nstruct Descending <: AbstractUserTerm end      # arcs i -> j with i > j\nname(::Descending) = \"descending\"\ncompute(::Descending, net) = Float64(count(src(e) > dst(e) for e in edges(net)))\nchange_stat(::Descending, net, i::Int, j::Int) = i > j ? 1.0 : 0.0\nnet = network(4; directed=true)\nadd_edge!(net, 3, 1); add_edge!(net, 1, 2)\ncompute(Descending(), net)                                            # 1.0\nvalidate_term(Descending(), net; verbose=false, rng=Xoshiro(1))       # true\n\n\n\n\n\n","title":"Networks.compute"},{"category":"function","location":"api/types/#ERGM.change_stat","page":"Types","text":"change_stat(term::AbstractERGMTerm, net, i::Int, j::Int) -> Float64\n\nCompute the add-direction change statistic for dyad (i, j): g(y⁺ᵢⱼ) − g(y⁻ᵢⱼ), i.e. the statistic with edge (i, j) present minus the statistic with it absent, holding all other dyads fixed.\n\nThe value must not depend on whether the edge currently exists: ERGM.jl's MPLE design matrix uses it directly, and the Metropolis–Hastings sampler negates it for removal proposals. The toggle-direction idiom has_edge(net, i, j) ? -delta : delta is wrong under this contract and is rejected by change_stat_check / validate_term.\n\nThis generic is owned by ERGM.jl and re-exported here (ERGMUserterms.change_stat === ERGM.change_stat); add a method for your term type after import ERGM: change_stat. For performance it should be O(degree), not O(edges).\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nimport ERGM: name, compute, change_stat\nstruct Mutuals <: AbstractUserTerm end          # mutual dyads, counted once\nname(::Mutuals) = \"mutuals\"\ncompute(::Mutuals, net) =\n    Float64(count(has_edge(net, i, j) && has_edge(net, j, i)\n                  for i in 1:nv(net) for j in 1:nv(net) if i < j))\n# Adding i -> j completes a mutual dyad iff j -> i is there; the value does\n# not consult has_edge(net, i, j) itself\nchange_stat(::Mutuals, net, i::Int, j::Int) = has_edge(net, j, i) ? 1.0 : 0.0\nnet = network(4; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 2, 1); add_edge!(net, 3, 4)\nchange_stat(Mutuals(), net, 4, 3)                                    # 1.0\nchange_stat(Mutuals(), net, 1, 2)                                    # 1.0 — edge present, same value\nchange_stat_check(Mutuals(), net; n_tests=8, rng=Xoshiro(1))         # true\n\n\n\n\n\n","title":"ERGM.change_stat"},{"category":"section","location":"guide/benchmarking/#Benchmarking","page":"Benchmarking","text":"ERGMUserterms.jl includes timing tools (benchmark_term, profile_term) to ensure custom terms are efficient enough for MCMC simulation. Since change_stat() is called thousands of times during ERGM estimation, its performance directly affects model fitting speed.","title":"Benchmarking"},{"category":"section","location":"guide/benchmarking/#Why-Benchmark?","page":"Benchmarking","text":"In ERGM estimation via MCMC:\n\nEach iteration proposes toggling one edge\nchange_stat() is called for every proposed toggle\nA typical estimation involves 10,000-1,000,000+ proposals\nSlow change_stat() makes estimation infeasible\n\nThe ideal change_stat() is much faster than compute(), because it only needs to calculate the local change rather than recomputing the full statistic.","title":"Why Benchmark?"},{"category":"section","location":"guide/benchmarking/#benchmark_term","page":"Benchmarking","text":"Time compute() and change_stat() (benchmark_term is a timer, not a profiler — for a call graph use Julia's Profile on your term directly):\n\n<!– skip-check –>\n\nresult = benchmark_term(term, net; n_iter=1000, rng=Random.default_rng())\n\nn_iter timed calls of each function; the change_stat calls hit random dyads drawn from rng, so the same rng benchmarks the same dyads (the timings themselves are, of course, whatever the machine makes of them).","title":"benchmark_term"},{"category":"section","location":"guide/benchmarking/#Parameters","page":"Benchmarking","text":"Parameter Description Default\nn_iter Timed calls of each function (must be ≥ 1) 1000\nrng Source of the benchmarked dyads; the same Xoshiro(seed) times the same dyads Random.default_rng()\n\nbenchmark_term throws an ArgumentError for n_iter < 1 or a network with fewer than two vertices (nothing to draw a dyad from).","title":"Parameters"},{"category":"section","location":"guide/benchmarking/#Return-Value","page":"Benchmarking","text":"The function returns a NamedTuple with:\n\nField Type Description\ncompute_mean Float64 Mean time for compute() (seconds)\ncompute_std Float64 Standard deviation of compute() time\nchange_stat_mean Float64 Mean time for change_stat() (seconds)\nchange_stat_std Float64 Standard deviation of change_stat() time\nspeedup Float64 Ratio: compute_mean / change_stat_mean","title":"Return Value"},{"category":"section","location":"guide/benchmarking/#Basic-Usage","page":"Benchmarking","text":"using ERGM, ERGMUserterms, Networks, Random\n\n# Create a test network\nrng = Xoshiro(1)\nnet = network(100; directed=true)\nfor _ in 1:500\n    i, j = rand(rng, 1:100), rand(rng, 1:100)\n    i != j && add_edge!(net, i, j)\nend\n\n# Benchmark\nterm = ExampleTerm()\nresult = benchmark_term(term, net; n_iter=1000, rng=rng)\n\nprintln(\"compute() mean:     $(round(result.compute_mean * 1e6, digits=2)) μs\")\nprintln(\"change_stat() mean: $(round(result.change_stat_mean * 1e6, digits=2)) μs\")\nprintln(\"Speedup:            $(round(result.speedup, digits=1))×\")","title":"Basic Usage"},{"category":"section","location":"guide/benchmarking/#Interpreting-Results","page":"Benchmarking","text":"Speedup Assessment Action\n> 100× Excellent Term is well-optimized\n10-100× Good Acceptable for most networks\n2-10× Moderate Consider optimizing for large networks\n~1× Poor change_stat likely recomputes the full statistic\n< 1× Very poor change_stat is slower than compute (bug?)","title":"Interpreting Results"},{"category":"section","location":"guide/benchmarking/#Performance-Guidelines","page":"Benchmarking","text":"","title":"Performance Guidelines"},{"category":"section","location":"guide/benchmarking/#Target-Complexity","page":"Benchmarking","text":"Function Target Complexity Description\ncompute() O(m) or O(m + n) Iterate over edges or vertices once\nchange_stat() O(d) or O(d²) Only examine neighbors of i and j\n\nWhere m = number of edges, n = number of vertices, d = average degree.","title":"Target Complexity"},{"category":"section","location":"guide/benchmarking/#Optimizing-change_stat()","page":"Benchmarking","text":"The key insight: when toggling edge (i,j), only statistics involving vertices i or j change. You only need to compute the local effect.","title":"Optimizing change_stat()"},{"category":"section","location":"guide/benchmarking/#Example:-Counting-Triangles","page":"Benchmarking","text":"<!– skip-check –>\n\n# SLOW: O(m) - recomputes full statistic\nfunction change_stat(::TriangleTerm, net, i::Int, j::Int)\n    # force edge absent, compute; force present, compute; restore\n    # ...\n    return with_edge - without_edge   # add-direction, but O(m)\nend\n\n# FAST: O(d) - only counts shared neighbors of i and j\n# (add-direction: no sign flip based on the dyad's current state)\nfunction change_stat(::TriangleTerm, net, i::Int, j::Int)\n    shared = 0\n    for k in outneighbors(net, i)\n        k != j && has_edge(net, j, k) && (shared += 1)\n    end\n    return Float64(shared)\nend","title":"Example: Counting Triangles"},{"category":"section","location":"guide/benchmarking/#Example:-Node-Match","page":"Benchmarking","text":"<!– skip-check –>\n\n# SLOW: O(m) - iterates all edges\nfunction change_stat(t::NodeMatchTerm, net, i::Int, j::Int)\n    before = compute(t, net)\n    # ...\n    return after - before\nend\n\n# SLOW in a different way: O(1) lookups, but the whole-Dict getter\n# allocates an empty default Dict on every call and infers Any\n# (see the pins below) — do not copy this\nfunction change_stat(t::NodeMatchTerm, net, i::Int, j::Int)\n    attrs = get_vertex_attribute(net, t.attr)\n    same = get(attrs, i, nothing) == get(attrs, j, nothing)\n    return same ? 1.0 : 0.0\nend\n\n# FAST: O(1), 0 B - the per-vertex getter returns `nothing` when absent\n# and allocates nothing; the result is a Bool, so inference closes\nfunction change_stat(t::NodeMatchTerm, net, i::Int, j::Int)\n    a = get_vertex_attribute(net, t.attr, i)\n    b = get_vertex_attribute(net, t.attr, j)\n    return (a !== nothing && a == b) ? 1.0 : 0.0\nend","title":"Example: Node Match"},{"category":"section","location":"guide/benchmarking/#Benchmarking-at-Different-Scales:-profile_term","page":"Benchmarking","text":"Network size significantly affects performance. profile_term runs benchmark_term on random networks of several sizes and returns one result per size, which makes change statistics that do not scale (e.g. O(edges) instead of O(degree)) easy to spot. The networks are built as test_term builds them: pass directed=false for an undirected-only term and vertex_attributes=Dict(:attr => v -> …) for an attribute-declaring one, so the term is timed on the branch a real fit would run:\n\nresults = profile_term(ExampleTerm(); sizes=[10, 50, 100], density=0.1, n_iter=500,\n                       rng=Xoshiro(1))\n\nfor r in results\n    println(\"n=$(r.n_vertices), m=$(r.ne): \" *\n            \"compute=$(round(r.compute_mean*1e6, digits=1))μs, \" *\n            \"change_stat=$(round(r.change_stat_mean*1e6, digits=1))μs, \" *\n            \"speedup=$(round(r.speedup, digits=1))×\")\nend\n\nEach entry is the benchmark_term NamedTuple for that size, with n_vertices (network size) and ne (realized edge count) added.\n\nParameter Description Default\nsizes Network sizes (vertex counts) to profile [10, 20, 40]\ndensity Edge density of each random test network 0.1\nn_iter Iterations passed to benchmark_term per size 200\nrng Source of every random draw (the networks and the benchmarked dyads) Random.default_rng()","title":"Benchmarking at Different Scales: profile_term"},{"category":"section","location":"guide/benchmarking/#Expected-Scaling-Behavior","page":"Benchmarking","text":"For a well-implemented term:\n\nNetwork Size compute() change_stat() Speedup\nn=10 ~1 μs ~0.1 μs ~10×\nn=100 ~50 μs ~1 μs ~50×\nn=1000 ~5 ms ~10 μs ~500×\n\nThe speedup should increase with network size because compute() scales with the network while change_stat() stays local.","title":"Expected Scaling Behavior"},{"category":"section","location":"guide/benchmarking/#Comparing-Terms","page":"Benchmarking","text":"Benchmark multiple terms on the same network to compare:\n\nterms = [\n    ExampleTerm(),\n    TemplateTerm(2.0),\n    WeightedEdges(),\n]\n\nrng = Xoshiro(1)\nnet = network(50; directed=true)\nfor _ in 1:200\n    i, j = rand(rng, 1:50), rand(rng, 1:50)\n    i != j && add_edge!(net, i, j)\nend\n\nfor term in terms\n    result = benchmark_term(term, net; n_iter=1000, rng=rng)\n    println(\"$(rpad(name(term), 20)) \" *\n            \"compute=$(round(result.compute_mean*1e6, digits=1))μs  \" *\n            \"change_stat=$(round(result.change_stat_mean*1e6, digits=1))μs  \" *\n            \"speedup=$(round(result.speedup, digits=1))×\")\nend","title":"Comparing Terms"},{"category":"section","location":"guide/benchmarking/#Profiling-Tips","page":"Benchmarking","text":"","title":"Profiling Tips"},{"category":"section","location":"guide/benchmarking/#Warm-Up-the-JIT","page":"Benchmarking","text":"Julia compiles functions on first call. Warm up before benchmarking:\n\n# Warm-up calls (not timed)\ncompute(term, net)\nchange_stat(term, net, 1, 2)\n\n# Now benchmark\nresult = benchmark_term(term, net; n_iter=1000, rng=Xoshiro(2))\n@assert result.compute_mean > 0\n\nbenchmark_term handles this internally by using many iterations, but be aware of it for manual timing.","title":"Warm Up the JIT"},{"category":"section","location":"guide/benchmarking/#Memory-Allocation","page":"Benchmarking","text":"Minimize allocations in change_stat():\n\n<!– skip-check –>\n\n# BAD: allocates on every call\nfunction change_stat(::MyTerm, net, i::Int, j::Int)\n    neighbors = collect(outneighbors(net, i))  # Allocates!\n    # ...\nend\n\n# GOOD: iterates without allocation\nfunction change_stat(::MyTerm, net, i::Int, j::Int)\n    for k in outneighbors(net, i)  # No allocation\n        # ...\n    end\nend","title":"Memory Allocation"},{"category":"section","location":"guide/benchmarking/#Type-Stability","page":"Benchmarking","text":"Ensure your functions are type-stable:\n\n<!– skip-check –>\n\n# BAD: type-unstable return\nfunction change_stat(::MyTerm, net, i::Int, j::Int)\n    if some_condition\n        return 1    # Returns Int\n    else\n        return 0.0  # Returns Float64\n    end\nend\n\n# GOOD: type-stable return\nfunction change_stat(::MyTerm, net, i::Int, j::Int)\n    if some_condition\n        return 1.0  # Always Float64\n    else\n        return 0.0  # Always Float64\n    end\nend","title":"Type Stability"},{"category":"section","location":"guide/benchmarking/#The-Package's-Own-Benchmark-Suite-and-Allocation-Pins","page":"Benchmarking","text":"benchmark/ is a standalone BenchmarkTools environment (its Project.toml sources this package as {path = \"..\"} and the siblings as ../../ERGM.jl, ../../Networks.jl):\n\njulia --project=benchmark -e 'using Pkg; Pkg.instantiate()'\njulia --project=benchmark benchmark/regression_tests.jl   # allocation gates (CI runs this)\njulia --project=benchmark benchmark/benchmarks.jl         # BENCHJL rows + scaling assertion\n\nbenchmarks.jl defines SUITE with one @benchmarkable per bundled term (and the template's ReciprocatedHomophily) for compute and for change_stat, on directed Erdős–Rényi networks with the same mean degree at n = 500 and n = 2000; run standalone it prints one BENCHJL\\t<name>\\t<median ns>\\t<allocs>\\t<bytes> line per benchmark (what the site's tools/run_benchmarks.jl consumes) and exits non-zero if any term's per-dyad change_stat cost grew by more than 3× between the two sizes — an O(edges) regression shows up as ≈4×, O(1)/O(degree) stays ≈1×.\n\nregression_tests.jl (mirrored by the test suite's \"Allocation regressions\" testset) pins two things for every bundled term and the template's ReciprocatedHomophily, on Julia 1.12.6: 0 B per change_stat and per compute, and Base.return_types(change_stat, …) == [Float64] (likewise compute).\n\nterm change_stat how\nExampleTerm, TemplateTerm, DyadCovTerm 0 B no attribute reads\nWeightedEdges 0 B get_edge_attribute(net, attr, i, j) per edge, asserted ::Float64\nInteractionTerm 0 B get_vertex_attribute(net, attr, v) per vertex, asserted ::Float64\nReciprocatedHomophily 0 B get_vertex_attribute(net, attr, v) per vertex\n\nThe inference pin is the important one. The attribute stores are untyped (Dict{Int,Any} / Dict{Tuple{Int,Int},Any}), so a term that reads them with a bare get(get_vertex_attribute(net, attr), v, 0.0) infers Any — which is what the shipped WeightedEdges/InteractionTerm did before 0.2.0: 96 B / 208 B of boxing per call, and, worse, ERGM's statically typed change-statistic tuple inferred Tuple{Float64, Any} for every model containing them, so each MPLE row and MH step went through a dynamic convert. The per-element getters return nothing when the value is absent, allocate nothing (the whole-Dict getters allocate an empty default Dict on every call), and the ::Float64 assertion closes inference.\n\nA term on a real hot path should still go one step further and snapshot its attributes once, typed, at construction — vertex_attribute_vector(net, attr, Float64) / get_edge_attribute(net, attr, Float64) — and index a Vector{Float64} in change_stat: a vector index instead of two hash lookups (see Attribute Validation and Snapshotting). Any allocation, or an Any return type, is a regression.","title":"The Package's Own Benchmark Suite and Allocation Pins"},{"category":"section","location":"guide/benchmarking/#Best-Practices","page":"Benchmarking","text":"Benchmark on realistic networks: Use sizes similar to your actual data\nAim for high speedup: change_stat should be at least 10× faster than compute\nCheck scaling: Speedup should increase with network size\nMinimize allocations: Avoid creating arrays or dictionaries in change_stat\nUse type-stable code: Return consistent types from all code paths\nProfile before optimizing: Identify the actual bottleneck first\nCompare with built-in terms: Your terms should be comparable to ERGM.jl's built-in terms\nRe-benchmark after changes: Performance can change with code modifications","title":"Best Practices"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"Implement a shared-neighbor statistic, verify its change calculation on several networks, and use it in a model. Keep the distinction between passing an implementation check and choosing a statistically well-posed model throughout the workflow.\n\nnote: Before you begin\nThe validator checks computational consistency on the networks it exercises. It does not establish identifiability, model fit, or scientific validity. A custom term must declare its dependence and attribute requirements accurately; missing-data support is opt-in.","title":"Getting Started"},{"category":"section","location":"getting_started/#Installation","page":"Getting Started","text":"<p>Use Julia <strong>1.12 or newer</strong> and the <a href=\"/getting-started/\">shared workspace installation guide</a>. These development packages are not yet registered; the guide prepares the required sibling checkouts and a Julia environment for the examples.</p>\n\nRun the blocks below in order in that environment. They build on variables from earlier steps; stochastic examples use seeded random number generators where shown.","title":"Installation"},{"category":"section","location":"getting_started/#Basic-Workflow","page":"Getting Started","text":"The typical ERGMUserterms.jl workflow consists of four steps:\n\nDefine the term - Create a struct and implement the interface\nValidate - Check correctness with automated tools\nTest - Run comprehensive tests on various networks\nUse in ERGM - Integrate with ERGM.jl for model estimation","title":"Basic Workflow"},{"category":"section","location":"getting_started/#Step-1:-Define-a-Custom-Term","page":"Getting Started","text":"Every custom term needs a struct and three methods:\n\nusing ERGM\nusing ERGMUserterms\nusing Networks\nusing Graphs: src, dst                    # edge endpoints (Graphs.jl's; not re-exported by Networks)\nimport ERGM: name, compute, change_stat   # required to extend the term interface\n\n# Define the struct\nstruct SharedNeighborTerm <: AbstractUserTerm end\n\n# Method 1: name\nname(::SharedNeighborTerm) = \"shared_neighbors\"\n\n# Method 2: compute - full network statistic\nfunction compute(::SharedNeighborTerm, net)\n    total = 0.0\n    for e in edges(net)\n        i, j = src(e), dst(e)\n        # Count common out-neighbors\n        for k in outneighbors(net, i)\n            k != j && has_edge(net, j, k) && (total += 1.0)\n        end\n    end\n    return total\nend\n\n# Method 3: change_stat - the ADD-DIRECTION change statistic: the value of\n# the statistic with edge (i,j) present minus with it absent, regardless of\n# the dyad's current state. Neighborhoods that could contain the dyad's own\n# edge are masked (k == j skips) so the value is state-independent.\nfunction change_stat(::SharedNeighborTerm, net, i::Int, j::Int)\n    delta = 0.0\n\n    # 1. The new edge (i,j)'s own shared out-neighbors\n    for k in outneighbors(net, i)\n        k == j && continue\n        has_edge(net, j, k) && (delta += 1.0)\n    end\n\n    # 2. Existing edges (i,b) gain shared out-neighbor j when b→j\n    for b in outneighbors(net, i)\n        b == j && continue\n        has_edge(net, b, j) && (delta += 1.0)\n    end\n\n    # 3. Existing edges (a,i) gain shared out-neighbor j when a→j\n    for a in inneighbors(net, i)\n        a == j && continue\n        has_edge(net, a, j) && (delta += 1.0)\n    end\n\n    return delta\nend","title":"Step 1: Define a Custom Term"},{"category":"section","location":"getting_started/#Term-Struct-Guidelines","page":"Getting Started","text":"Guideline Description\nSubtype AbstractUserTerm Ensures compatibility with validation tools\nStore parameters as fields Any configuration the term needs\nUse type parameters for flexibility e.g., TemplateTerm{T} for generic parameters\nKeep structs immutable Use struct, not mutable struct","title":"Term Struct Guidelines"},{"category":"section","location":"getting_started/#Step-2:-Validate-the-Term","page":"Getting Started","text":"Use validate_term to check your implementation:\n\n# Create a test network\nnet = network(10; directed=true)\nfor (i, j) in [(1,2), (2,3), (1,3), (3,1), (2,1)]\n    add_edge!(net, i, j)\nend\n\n# Run validation (seeded: the same dyads are checked every time, and any\n# failure message ends with an rng literal that replays it)\nusing Random\nterm = SharedNeighborTerm()\nvalid = validate_term(term, net; verbose=true, rng=Xoshiro(1))\n@assert valid    # the page's claim, checked: a rotten example fails here\n\n# Output:\n# [ Info: ✓ name() returns: shared_neighbors\n# [ Info: ✓ compute() returns: 3.0 (Float64)\n# [ Info: Testing change_stat() with 10 random dyads...\n# [ Info: ✓ change_stat() returns valid values\n# [ Info: Running consistency check...\n# [ Info: ✓ change_stat() consistent with compute()\n# [ Info: ✓ declared attributes match the ones the term reads\n# [ Info: ✓ accepted by ERGMModel construction","title":"Step 2: Validate the Term"},{"category":"section","location":"getting_started/#Validation-Checks","page":"Getting Started","text":"Check Description\nname() Returns a non-empty String\ncompute() Returns a Real value without errors\nchange_stat() Returns Real values for n_tests random dyads\nConsistency change_stat() matches compute() differences on n_tests random dyads\nTraits The term's declarations hold (validate_traits) and ERGMModel accepts it\n\nEvery random dyad comes from the rng keyword (default Random.default_rng()); pass rng=Xoshiro(seed) for a run you can repeat, and read the rng=Xoshiro(…) literal off any failure message to replay that exact run.","title":"Validation Checks"},{"category":"section","location":"getting_started/#Step-3:-Test-Comprehensively","page":"Getting Started","text":"Use test_term for thorough testing on random networks. The networks it generates are directed and carry no vertex attributes unless you say otherwise: a term that declares ERGM.required_vertex_attributes needs vertex_attributes=Dict(:attr => v -> value) (or a vector with one value per vertex), and one that declares ERGM.requires_undirected needs directed=false — otherwise validate_term reports the mismatch and the run fails, as it should:\n\npassed = test_term(term; n_vertices=20, density=0.1, n_tests=100, rng=Xoshiro(1))\n@assert passed\n\n# A nodematch-style term declaring `ERGM.required_vertex_attributes(t) = (t.attr,)`\n# is tested on networks that carry the attribute:\n@assert test_term(InteractionTerm(:a, :b); n_vertices=12, n_tests=20, rng=Xoshiro(1),\n                  vertex_attributes=Dict(:a => (v -> Float64(v)), :b => (v -> Float64(13 - v))))\n\n# Output:\n# Testing term: shared_neighbors\n# (replay with rng=Xoshiro(0x…, 0x…, 0x…, 0x…))\n# ==================================================\n# [ Info: ✓ name() returns: shared_neighbors\n# [ Info: ✓ compute() returns: 6.0 (Float64)\n# [ Info: Testing change_stat() with 100 random dyads...\n# [ Info: ✓ change_stat() returns valid values\n# [ Info: Running consistency check...\n# [ Info: ✓ change_stat() consistent with compute()\n# [ Info: ✓ declared attributes match the ones the term reads\n# [ Info: ✓ accepted by ERGMModel construction\n#\n# Additional tests:\n# ✓ Works on empty network: 0.0\n# ✓ Works on complete network: 720.0\n# ==================================================\n# All tests PASSED","title":"Step 3: Test Comprehensively"},{"category":"section","location":"getting_started/#Test-Parameters","page":"Getting Started","text":"Parameter Description Default\nn_vertices Number of vertices in random test network 20\ndensity Edge density of random test network 0.1\nn_tests Random dyads for the change_stat checks (passed to validate_term) 100\ndirected Build directed (true) or undirected (false) networks — false for a requires_undirected term true\nvertex_attributes attr => spec pairs set on every generated network; spec is v -> value or a vector with one value per vertex Dict{Symbol,Any}()\nrng Source of every random draw — the network and the dyads Random.default_rng()\n\nTwo things test_term (and every validator) will not do: validate on a two-mode network — network(n; bipartite=k) is refused with an ArgumentError, because ERGM.jl fits one-mode networks only and the within-mode dyads cannot hold an edge — and treat a masked dyad as anything but its face value for a term that does not declare Networks.supports_missing (it logs one [ Info: net has k masked dyads …] line so you know; ERGM.jl applies its missing= policy at estimation time).","title":"Test Parameters"},{"category":"section","location":"getting_started/#Step-4:-Use-in-ERGM","page":"Getting Started","text":"Once validated, your term works with ERGM.jl exactly like a built-in one — in a term list, in ERGMModel construction, and in fit_ergm (or its statnet-style alias ergm, the same function):\n\nusing ERGM\nusing Networks\nusing Random\n\n# Create a network\nrng = Xoshiro(2)\nnet = network(50; directed=true)\nfor _ in 1:150\n    i, j = rand(rng, 1:50), rand(rng, 1:50)\n    i != j && add_edge!(net, i, j)\nend\n\n# Use your custom term next to built-ins\nterms = [Edges(), Triangle(), SharedNeighborTerm()]\n\n# Compute statistics\nfor term in terms\n    println(name(term), \": \", compute(term, net))\nend\n\n# Fit a model containing it (MPLE by default; pass method=:mcmle for a\n# Monte-Carlo MLE, and always an rng)\nfit = fit_ergm(net, [Edges(), SharedNeighborTerm()]; rng=Xoshiro(3))\ncoef(fit)                 # two coefficients: edges, shared_neighbors\n@assert length(coef(fit)) == 2\n@assert ergm === fit_ergm","title":"Step 4: Use in ERGM"},{"category":"section","location":"getting_started/#Complete-Example","page":"Getting Started","text":"using ERGM\nusing ERGMUserterms\nusing Networks\nusing Random\nimport ERGM: name, compute, change_stat   # required to extend the term interface\n\n# === Define a parameterized term ===\nstruct WeightedDensity <: AbstractUserTerm\n    weight::Float64\nend\n\nname(t::WeightedDensity) = \"wdensity.$(t.weight)\"\n\nfunction compute(t::WeightedDensity, net)\n    n = nv(net)\n    max_edges = n * (n - 1)\n    return max_edges > 0 ? t.weight * ne(net) / max_edges : 0.0\nend\n\nfunction change_stat(t::WeightedDensity, net, i::Int, j::Int)\n    # Add-direction: one extra edge adds weight/max_edges\n    n = nv(net)\n    max_edges = n * (n - 1)\n    max_edges == 0 && return 0.0\n    return t.weight / max_edges\nend\n\n# === Validate ===\nrng = Xoshiro(1)\nnet = network(20; directed=true)\nfor _ in 1:38\n    i, j = rand(rng, 1:20), rand(rng, 1:20)\n    i != j && add_edge!(net, i, j)\nend\n\nterm = WeightedDensity(10.0)\nvalid = validate_term(term, net; verbose=true, rng=rng)\n@assert valid\n\n# === Run comprehensive tests ===\n@assert test_term(term; n_vertices=30, density=0.15, rng=rng)\n\n# === Benchmark performance ===\nresult = benchmark_term(term, net; n_iter=1000, rng=rng)\nprintln(\"compute() mean: \", round(result.compute_mean * 1e6, digits=2), \" μs\")\nprintln(\"change_stat() mean: \", round(result.change_stat_mean * 1e6, digits=2), \" μs\")\nprintln(\"Speedup: \", round(result.speedup, digits=1), \"×\")\n\n# === Use in model ===\nif valid\n    println(\"\\nTerm ready for ERGM estimation!\")\n    println(\"Statistic value: \", compute(term, net))\nend","title":"Complete Example"},{"category":"section","location":"getting_started/#Working-with-the-@ergm_term-Macro","page":"Getting Started","text":"The @ergm_term macro checks, right after the definition, that the type subtypes AbstractUserTerm and has its own name/compute/change_stat methods (a bare using without the import ERGM: … line above would leave local functions behind, and the macro warns about each missing method):\n\n@ergm_term MyDegreeVar begin\n    struct MyDegreeVar <: AbstractUserTerm end\n\n    name(::MyDegreeVar) = \"degree_variance\"\n\n    function compute(::MyDegreeVar, net)\n        n = nv(net)\n        n == 0 && return 0.0\n        degrees = [length(outneighbors(net, v)) for v in vertices(net)]\n        mean_deg = sum(degrees) / n\n        return sum((d - mean_deg)^2 for d in degrees) / n\n    end\n\n    function change_stat(::MyDegreeVar, net, i::Int, j::Int)\n        # Add-direction by brute force: statistic with edge (i,j) forced\n        # present minus forced absent, then restore the original state.\n        # (Fine for a small demo; real terms should compute the delta\n        # directly in O(degree) — see the efficiency guidelines.)\n        had = has_edge(net, i, j)\n        had && rem_edge!(net, i, j)\n        without = compute(MyDegreeVar(), net)\n        add_edge!(net, i, j)\n        with = compute(MyDegreeVar(), net)\n        had || rem_edge!(net, i, j)\n        return with - without\n    end\nend\n\n@assert validate_term(MyDegreeVar(), net; verbose=false, rng=Xoshiro(4))","title":"Working with the @ergm_term Macro"},{"category":"section","location":"getting_started/#Best-Practices","page":"Getting Started","text":"Start with a template: Copy an example term and modify it\nValidate early: Run validate_term after implementing each method\nTest edge cases: Verify on empty and complete networks\nOptimize change_stat: It should be O(degree), not O(edges)\nUse descriptive names: Include parameters in the name string\nDocument your terms: Use term_documentation() for auto-generated docs","title":"Best Practices"},{"category":"section","location":"getting_started/#Next-Steps","page":"Getting Started","text":"Learn the Term Interface in detail\nStudy the Templates and Examples provided\nMaster Validation and Testing tools\nProfile with Benchmarking utilities","title":"Next Steps"},{"category":"section","location":"guide/term_interface/#Term-Interface","page":"Term Interface","text":"This guide covers the ERGM term interface in detail, explaining how terms work and what your custom implementation must satisfy.","title":"Term Interface"},{"category":"section","location":"guide/term_interface/#The-Three-Required-Methods","page":"Term Interface","text":"Every ERGM term must implement exactly three methods:\n\n<!– skip-check –>\n\nname(term) -> String\ncompute(term, net) -> Float64\nchange_stat(term, net, i, j) -> Float64\n\nThese methods form a contract that ERGM.jl relies on for correct model estimation.\n\nThe examples below assume a term struct like this has been defined:\n\nusing ERGM, ERGMUserterms, Networks, Random\nusing Graphs: src, dst                    # edge endpoints are Graphs.jl's\nimport ERGM: name, compute, change_stat   # the shared generics (ERGMUserterms re-exports the same ones)\n\nstruct MyTerm <: AbstractUserTerm\n    param::Float64\nend\n\nt = MyTerm(2.0)\nnet = network(10; directed=true)\n\nThe import line is not optional: name, compute and change_stat are ERGM.jl's generics (compute and name are Networks.jl's shared statistic protocol), and ERGMUserterms.jl re-exports the very same functions — ERGMUserterms.compute === ERGM.compute === Networks.compute. A method added after a bare using defines a rival local function that ERGM.jl never calls. src/dst are Graphs.src/Graphs.dst, which neither Networks.jl nor ERGM.jl re-exports, so a term iterating edges(net) imports them.","title":"The Three Required Methods"},{"category":"section","location":"guide/term_interface/#name()","page":"Term Interface","text":"Returns a human-readable string identifier for the term — \"myterm\" for a term without parameters, and with the parameters spelled out otherwise:\n\nname(t::MyTerm) = \"myterm.$(t.param)\"","title":"name()"},{"category":"section","location":"guide/term_interface/#Requirements","page":"Term Interface","text":"Requirement Description\nNon-empty Must return a non-empty string\nDescriptive Should identify the term and its configuration\nUnique Different configurations should produce different names\nDeterministic Must return the same value every time","title":"Requirements"},{"category":"section","location":"guide/term_interface/#Naming-Conventions","page":"Term Interface","text":"<!– skip-check –>\n\n# Simple term\nname(::Edges) = \"edges\"\n\n# Parameterized term (R ergm's label, so coefficient tables line up with statnet)\nname(t::GWDegree) = \"gwdeg.fixed.$(t.decay)\"\n\n# Attribute-based term\nname(t::NodeMatch) = \"nodematch.$(t.attr)\"\n\n# Multiple parameters\nname(t::InteractionTerm) = \"interact.$(t.attr1).$(t.attr2)\"\n\nERGM.jl also has a two-argument name(term, net) for labels that depend on the network's direction (name(GWESP(0.5), net) is \"gwesp.fixed.0.5\" on an undirected network and \"gwesp.OTP.fixed.0.5\" on a directed one, as in statnet). It falls back to name(term), so a user term needs only the one-argument method; add the two-argument one only if your label really depends on the network.","title":"Naming Conventions"},{"category":"section","location":"guide/term_interface/#compute()","page":"Term Interface","text":"Calculates the full network statistic value:\n\nfunction compute(term::MyTerm, net)\n    # Iterate over edges, vertices, etc.\n    # Return the statistic value as a Float64\nend","title":"compute()"},{"category":"section","location":"guide/term_interface/#Requirements-2","page":"Term Interface","text":"Requirement Description\nReturns Real Must return a Real (typically Float64)\nDeterministic Same network must always produce the same value\nNo side effects Must not modify the network\nHandles edge cases Should work on empty and complete networks","title":"Requirements"},{"category":"section","location":"guide/term_interface/#Common-Patterns","page":"Term Interface","text":"<!– skip-check –>\n\n# Counting edges with a property\nfunction compute(::MyEdgeTerm, net)\n    count = 0.0\n    for e in edges(net)\n        # Check some condition\n        count += 1.0\n    end\n    return count\nend\n\n# Summing over vertices\nfunction compute(::MyNodeTerm, net)\n    total = 0.0\n    for v in vertices(net)\n        total += length(outneighbors(net, v))^2\n    end\n    return total\nend\n\n# Using attributes. get_vertex_attribute returns an *empty Dict* (never\n# `nothing`) when the attribute is absent. DECLARE the attribute\n# (`ERGM.required_vertex_attributes(t::MyAttrTerm) = (t.attr,)`): then an\n# ERGMModel refuses a network on which it is absent or set on only some\n# vertices, and the `get(attrs, v, 0.0)` fallback below is reached only by\n# raw compute calls — see \"Missing Attribute Values (NA)\" at the bottom\nfunction compute(t::MyAttrTerm, net)\n    attrs = get_vertex_attribute(net, t.attr)\n    total = 0.0\n    for e in edges(net)\n        total += get(attrs, src(e), 0.0) * get(attrs, dst(e), 0.0)\n    end\n    return total\nend","title":"Common Patterns"},{"category":"section","location":"guide/term_interface/#change_stat()","page":"Term Interface","text":"Calculates the add-direction change statistic for dyad (i,j):\n\nfunction change_stat(term::MyTerm, net, i::Int, j::Int)\n    # Return compute(net with edge i→j) - compute(net without edge i→j),\n    # holding every other dyad fixed. The value must NOT depend on whether\n    # the edge currently exists.\nend","title":"change_stat()"},{"category":"section","location":"guide/term_interface/#The-Fundamental-Relationship","page":"Term Interface","text":"change_stat(term, net, i, j) == compute(term, net⁺ij) - compute(term, net⁻ij)\n\nWhere net⁺ij/net⁻ij are the network with edge (i,j) forced present/absent and all other dyads unchanged.\n\nThis relationship must hold exactly, for both current states of the dyad, for correct ERGM estimation: the MPLE design matrix uses the value directly and the Metropolis–Hastings sampler negates it for removal proposals. ERGMUserterms.jl validates both the value and its state-independence with change_stat_check.","title":"The Fundamental Relationship"},{"category":"section","location":"guide/term_interface/#Requirements-3","page":"Term Interface","text":"Requirement Description\nReturns Real Must return a Real (typically Float64)\nConsistent with compute Must satisfy the fundamental relationship\nState-independent Same value whether the edge currently exists or not\nEfficient Should be O(degree), not O(edges)","title":"Requirements"},{"category":"section","location":"guide/term_interface/#Sign-Convention","page":"Term Interface","text":"Always return the add-direction value — do not flip the sign based on the dyad's current state (the estimation machinery handles removals itself):\n\n<!– skip-check –>\n\nfunction change_stat(::MyTerm, net, i::Int, j::Int)\n    # The contribution edge (i,j) makes when present. If the statistic\n    # depends on degrees or adjacency, evaluate them with the dyad's own\n    # edge masked out (the baseline state without i→j).\n    return compute_local_contribution(net, i, j)\nend\n\nwarning: Common bug\nThe idiom has_edge(net, i, j) ? -value : value (toggle-direction) is wrong for ERGM.jl: it double-negates removal proposals in the MH sampler and sign-flips the MPLE design rows of observed edges. It is, however, R ergm's own C convention — C_CHANGESTAT_FN(…, Rboolean edgestate) in ergm_changestat.h, with every statnet changestat written CHANGE_STAT[0] += edgestate ? -1 : 1 — so it is exactly what a port of an ergm.userterms changestat arrives with. When porting, drop the edgestate sign and return the add-direction value; ERGM.jl applies the sign for removals itself.","title":"Sign Convention"},{"category":"section","location":"guide/term_interface/#Efficiency-Guidelines","page":"Term Interface","text":"change_stat() is called thousands of times during MCMC simulation. It must be fast:\n\n<!– skip-check –>\n\n# GOOD: O(degree) - only examine neighbors of i and j\nfunction change_stat(::TriangleTerm, net, i::Int, j::Int)\n    shared = 0\n    for k in outneighbors(net, i)\n        k == j && continue\n        has_edge(net, j, k) && (shared += 1)\n    end\n    return Float64(shared)\nend\n\n# BAD: O(n^2) - examines all pairs\nfunction change_stat(::TriangleTerm, net, i::Int, j::Int)\n    before = compute(TriangleTerm(), net)  # O(n^2)!\n    # ... toggle and compute again\nend","title":"Efficiency Guidelines"},{"category":"section","location":"guide/term_interface/#Declaring-Your-Term's-Traits","page":"Term Interface","text":"Implementing name/compute/change_stat makes a term computable. Declaring its traits makes it a first-class citizen: ERGM.jl's formula validation and materialization act on the trait declarations, not on ERGM's own term types, so a term from any package is checked exactly like a built-in one at ERGMModel construction.\n\ntrait default declare it when\nERGM.required_vertex_attributes(t) () the term reads a vertex attribute and is meaningless without it\nERGM.required_edge_attributes(t) () likewise for an edge attribute\nERGM.requires_directed(t) false the statistic is undefined on undirected networks (ERGM's Mutual, OStar, GWODegree, … declare it)\nERGM.requires_undirected(t) false ... or on directed ones (ERGM's Degree, Kstar, GWDegree declare it)\nERGM.is_dyad_dependent(t) true the change statistic never reads another dyad → declare false\nNetworks.supports_missing(t) false the statistic consults is_missing_dyad and ignores masked dyads (ERGM.supports_missing is the same function)\n\n<!– skip-check –>\n\nstruct Homophily <: AbstractUserTerm\n    attr::Symbol\nend\n\nERGM.required_vertex_attributes(t::Homophily) = (t.attr,)\nERGM.is_dyad_dependent(::Homophily) = false\n\nWith that declaration, ERGMModel(ERGMFormula([Edges(), Homophily(:groop)]), net) throws an ArgumentError naming the misspelled attribute. Without it, the term would read an empty attribute Dict, evaluate to a constant zero, and produce a meaningless coefficient — silently.\n\nwarning: Declare an attribute only if its absence is an error\n\"Required\" means the model cannot be fitted without it. A term that reads an attribute if present and falls back to a default — like WeightedEdges, whose edges without a :weight count at t.default — genuinely does not require it, and declaring it would reject networks the term handles perfectly well.\n\nvalidate_term exercises every declaration (see validate_traits): it perturbs the network's attributes to check that the ones you declared are the ones your term actually reads, checks that a direction requirement is enforced by ERGMModel, toggles other dyads to check a dyad-independence claim, and flips the face value of masked dyads to check a supports_missing claim. The package template in examples/MyTermPackage/ is a complete term declaring all four.","title":"Declaring Your Term's Traits"},{"category":"section","location":"guide/term_interface/#supports_missing:-the-same-idiom-as-ERGM's-own-terms","page":"Term Interface","text":"Networks.supports_missing and ERGM.supports_missing are one function (ERGM.supports_missing === Networks.supports_missing), so either spelling extends the same generic. Declare true only if the statistic consults the mask itself — the same ObservedEdges idiom ERGM.jl's own supports_missing docstring uses:\n\nstruct ObservedEdges <: AbstractUserTerm end\nname(::ObservedEdges) = \"observed_edges\"\nfunction compute(::ObservedEdges, net)\n    total = 0.0\n    for e in edges(net)\n        is_missing_dyad(net, src(e), dst(e)) && continue   # the mask, not the face value\n        total += 1.0\n    end\n    return total\nend\nchange_stat(::ObservedEdges, net, i::Int, j::Int) = is_missing_dyad(net, i, j) ? 0.0 : 1.0\nNetworks.supports_missing(::ObservedEdges) = true    # ≡ ERGM.supports_missing(::ObservedEdges) = true\n\nmasked = network(5; directed=true)\nadd_edge!(masked, 1, 2); add_edge!(masked, 2, 3)\nset_missing_dyad!(masked, 1, 2)\ncompute(ObservedEdges(), masked)           # 1.0 — the masked tie does not count\n@assert compute(ObservedEdges(), masked) == 1.0\n@assert validate_traits(ObservedEdges(), masked; verbose=false, rng=Xoshiro(1))\n\nvalidate_traits checks the claim by flipping the face value of a masked dyad: the statistic of a term declaring true must not move. The default false is the honest answer for a term that counts edges as stored — that is what every ERGM.jl built-in declares, and the missing-data treatment then lives in the estimator (MPLE drops masked dyads from the pseudo-likelihood).","title":"supports_missing: the same idiom as ERGM's own terms"},{"category":"section","location":"guide/term_interface/#is_dyad_dependent-in-detail","page":"Term Interface","text":"ERGM.jl classifies every term as dyad-dependent or dyad-independent with ERGM.is_dyad_dependent(term). The classification matters in two places:\n\nMPLE honesty caveat: pseudo-likelihood fits containing any dyad-dependent term print a standard-error warning in show.\nMCMLE log-likelihood: the bridge-sampling reference distribution zeroes the coefficients of dyad-dependent terms, so a misclassified term corrupts the reported log-likelihood/AIC/BIC.\n\nFor unknown term types the fallback is true — the conservative answer. If your term's change statistic depends only on exogenous covariates of dyad (i,j) (never on the state of other dyads), declare it:\n\n<!– skip-check –>\n\nERGM.is_dyad_dependent(::MyCovariateTerm) = false\n\nwarning: Subtyping `NodalTerm`/`DyadicTerm` implies dyad-independence\nERGM.jl defines is_dyad_dependent(::NodalTerm) = false and is_dyad_dependent(::DyadicTerm) = false. Subtype those only for genuinely covariate-only terms; a term whose change statistic reads other dyads (degrees, shared partners, reciprocity) must not use them — keep AbstractUserTerm (fallback true) or add an explicit is_dyad_dependent method returning true.","title":"is_dyad_dependent in detail"},{"category":"section","location":"guide/term_interface/#Attribute-Validation-and-Snapshotting","page":"Term Interface","text":"ERGMModel construction validates every term — built-in or not — against the network, using the attributes the term declares (see Declaring Your Term's Traits above): a declared vertex or edge attribute that the network does not have throws an ArgumentError before any fitting, and so does a declared vertex attribute with a value on only some vertices (statnet refuses NA — see Missing Attribute Values (NA) below). The check is ERGM._validate_formula, public and documented in ERGM.jl. A term that declares nothing is not validated, so an undeclared attribute-based term will read an empty Dict and evaluate to zero — declare your attributes.\n\nTyped snapshotting (materialization into dense Vector{Float64}/code vectors) is still done only for ERGM.jl's own nodal terms. For hot loops, do that optimization yourself: read attributes once into a typed container in your term's constructor (or use Networks.jl's typed accessors vertex_attribute_vector(net, attr, V) / get_edge_attribute(net, attr, V)). Short of that, read per element — get_vertex_attribute(net, attr, v) / get_edge_attribute(net, attr, i, j) return nothing when the value is absent and allocate nothing — and assert the value's type (Float64(x)::Float64), as the bundled WeightedEdges/InteractionTerm do; never get(…) from the whole-Dict getters in change_stat, whose Dict{…,Any} makes the method infer Any and box on every sampler step. (The typed snapshot is faster still: a vector index instead of a hash lookup in every change_stat call.)","title":"Attribute Validation and Snapshotting"},{"category":"section","location":"guide/term_interface/#Type-Hierarchy","page":"Term Interface","text":"AbstractERGMTerm (from ERGM.jl)\n├── StructuralTerm\n├── NodalTerm\n├── DyadicTerm\n└── AbstractUserTerm (from ERGMUserterms.jl)","title":"Type Hierarchy"},{"category":"section","location":"guide/term_interface/#AbstractUserTerm","page":"Term Interface","text":"The base type for all user-defined terms:\n\n<!– skip-check –>\n\nabstract type AbstractUserTerm <: AbstractERGMTerm end\n\nYour terms should subtype AbstractUserTerm:\n\n<!– skip-check –>\n\nstruct MyTerm <: AbstractUserTerm\n    # fields\nend\n\nThis provides:\n\nCompatibility with all ERGM.jl functions\nAccess to ERGMUserterms.jl validation tools\nIntegration with the @ergm_term macro","title":"AbstractUserTerm"},{"category":"section","location":"guide/term_interface/#Choosing-the-Right-Supertype","page":"Term Interface","text":"Supertype When to Use\nAbstractUserTerm Default choice for custom terms (is_dyad_dependent falls back to true)\nStructuralTerm Pure structural terms (no attributes)\nNodalTerm Covariate-only vertex-attribute terms (implies dyad-independence)\nDyadicTerm Covariate-only edge/dyadic-attribute terms (implies dyad-independence)\n\nFor most custom terms, AbstractUserTerm is the right choice. Remember that NodalTerm/DyadicTerm carry the is_dyad_dependent = false trait (see above) — never use them for terms whose change statistic reads other dyads.\n\nA supertype does not fix a term's direction requirement; a declaration does. ERGM.jl's own Degree, Kstar and GWDegree are the built-in examples of requires_undirected = true — exactly the terms R ergm refuses on a directed network — and they refuse a directed network with one message naming the directed variant to use instead: ERGMModel (and compute / change_stat themselves) throw term 'kstar2' is only defined for undirected networks, but the network is directed. Use OStar(2) / IStar(2) … (GWODegree/GWIDegree for GWDegree, ODegree/IDegree for Degree). A user term declaring ERGM.requires_undirected(::MyTerm) = true gets the same refusal from ERGMModel — that is the built-in analogue of the declaration, and validate_traits checks it is enforced.","title":"Choosing the Right Supertype"},{"category":"section","location":"guide/term_interface/#Struct-Design","page":"Term Interface","text":"","title":"Struct Design"},{"category":"section","location":"guide/term_interface/#Simple-Terms-(No-Parameters)","page":"Term Interface","text":"struct MySimpleTerm <: AbstractUserTerm end","title":"Simple Terms (No Parameters)"},{"category":"section","location":"guide/term_interface/#Parameterized-Terms","page":"Term Interface","text":"struct MyParamTerm <: AbstractUserTerm\n    decay::Float64\n    normalize::Bool\nend\n\n# Constructor with defaults\nMyParamTerm(; decay=0.5, normalize=true) = MyParamTerm(decay, normalize)","title":"Parameterized Terms"},{"category":"section","location":"guide/term_interface/#Generic-Terms","page":"Term Interface","text":"struct MyGenericTerm{T} <: AbstractUserTerm\n    param::T\n    attr::Symbol\nend\n\nMyGenericTerm(param; attr=:none) = MyGenericTerm(param, attr)","title":"Generic Terms"},{"category":"section","location":"guide/term_interface/#Attribute-Based-Terms","page":"Term Interface","text":"struct MyAttrTerm <: AbstractUserTerm\n    attr::Symbol\nend\n\nstruct MyDyadTerm <: AbstractUserTerm\n    covariate::Matrix{Float64}\nend","title":"Attribute-Based Terms"},{"category":"section","location":"guide/term_interface/#Working-with-Networks","page":"Term Interface","text":"","title":"Working with Networks"},{"category":"section","location":"guide/term_interface/#Accessing-Network-Structure","page":"Term Interface","text":"<!– skip-check –>\n\n# Vertices\nnv(net)              # Number of vertices\nvertices(net)        # Iterator over vertex IDs\n\n# Edges\nne(net)              # Number of edges\nedges(net)           # Iterator over edges\nhas_edge(net, i, j)  # Check if edge exists\n\n# Neighbors\noutneighbors(net, v) # Outgoing neighbors\ninneighbors(net, v)  # Incoming neighbors\n\n# Edge endpoints\nsrc(e)               # Source of edge\ndst(e)               # Destination of edge","title":"Accessing Network Structure"},{"category":"section","location":"guide/term_interface/#Accessing-Attributes","page":"Term Interface","text":"<!– skip-check –>\n\n# Vertex attributes\nattrs = get_vertex_attribute(net, :name)\n# Returns Dict{Int, Any}; an *empty* Dict (never nothing) when the\n# attribute is absent\n\n# Edge attributes\nweights = get_edge_attribute(net, :weight)\n# Returns Dict{Tuple{Int,Int}, Any}; an *empty* Dict (never nothing) when\n# the attribute is absent. Keys are canonical: (i,j) for directed\n# networks, (min,max) for undirected\n\n# Network attributes\nval = get_network_attribute(net, :name)","title":"Accessing Attributes"},{"category":"section","location":"guide/term_interface/#Modifying-Networks-(Only-in-Tests)","page":"Term Interface","text":"<!– skip-check –>\n\n# Add/remove edges\nadd_edge!(net, i, j)\nrem_edge!(net, i, j)\n\n# NEVER modify the network inside compute() or change_stat()","title":"Modifying Networks (Only in Tests)"},{"category":"section","location":"guide/term_interface/#Edge-Cases-to-Handle","page":"Term Interface","text":"","title":"Edge Cases to Handle"},{"category":"section","location":"guide/term_interface/#Empty-Networks","page":"Term Interface","text":"<!– skip-check –>\n\nfunction compute(::MyTerm, net)\n    ne(net) == 0 && return 0.0  # Handle empty network\n    # ... normal computation\nend","title":"Empty Networks"},{"category":"section","location":"guide/term_interface/#Complete-Networks","page":"Term Interface","text":"Your term should handle networks where all possible edges exist.","title":"Complete Networks"},{"category":"section","location":"guide/term_interface/#Single-Vertex-Networks","page":"Term Interface","text":"<!– skip-check –>\n\nfunction compute(::MyTerm, net)\n    nv(net) < 2 && return 0.0  # Need at least 2 vertices\n    # ...\nend\n\n(compute may be called on such a network directly; the harness itself refuses one — with fewer than 2 vertices there is no dyad to check, so validate_term and friends throw an ArgumentError rather than report a verdict resting on nothing.)","title":"Single-Vertex Networks"},{"category":"section","location":"guide/term_interface/#Missing-Attribute-Values-(NA)","page":"Term Interface","text":"get_vertex_attribute/get_edge_attribute return an empty Dict (never nothing) when the attribute is absent, and vertices may lack individual values. What that means for your term is settled by the contract, not by the term:\n\nA declared vertex attribute (ERGM.required_vertex_attributes) must be set on every vertex, or ERGMModel construction throws — the message is ERGM's own: term 'attrsum.a' needs vertex attribute :a on every vertex, but 1 of 4 vertices have no value (vertices 4). statnet refuses NA attribute values; set a value for every vertex or drop the term. The check is ERGM._validate_formula (public, documented in ERGM.jl), the same one that validates ERGM's built-in attribute terms.\nAn undeclared attribute read is a validate_traits failure: on a network lacking the attribute the term would silently become an all-zero design column.\nA get(attrs, v, default) fallback in your code is therefore only ever reached by raw compute/change_stat calls outside a model. Keep it (it makes the term total on any network), but do not rely on it to define what a missing value means.\n\nstruct MyAttrSum <: AbstractUserTerm\n    attr::Symbol\nend\nname(t::MyAttrSum) = \"attrsum.$(t.attr)\"\n# One vertex's value, read per element (0 B, infers Float64); the zero-fill\n# is reached only by raw compute calls — inside an ERGMModel the declared\n# attribute is complete\nfunction _attr_value(t::MyAttrSum, net, v::Int)\n    x = get_vertex_attribute(net, t.attr, v)\n    return x === nothing ? 0.0 : Float64(x)::Float64\nend\nfunction compute(t::MyAttrSum, net)\n    total = 0.0\n    for e in edges(net)\n        total += _attr_value(t, net, Int(src(e))) + _attr_value(t, net, Int(dst(e)))\n    end\n    return total\nend\nchange_stat(t::MyAttrSum, net, i::Int, j::Int) =\n    _attr_value(t, net, i) + _attr_value(t, net, j)\nERGM.required_vertex_attributes(t::MyAttrSum) = (t.attr,)\nERGM.is_dyad_dependent(::MyAttrSum) = false\n\npartial = network(4; directed=true)\nadd_edge!(partial, 1, 2)\nset_vertex_attribute!(partial, :a, Dict(1 => 1.0, 2 => 2.0, 3 => 3.0))   # vertex 4: no value\ncompute(MyAttrSum(:a), partial)                       # 3.0 — the raw call zero-fills\nerr = try\n    ERGMModel(ERGMFormula([MyAttrSum(:a)]), partial)  # the model refuses\n    nothing\ncatch e\n    e\nend\n@assert err isa ArgumentError\n@assert occursin(\"on every vertex\", err.msg)\n# validate_traits reports the vertex without a value (quietly here) and fails\n@assert !validate_traits(MyAttrSum(:a), partial; verbose=false, rng=Xoshiro(1))\n\nset_vertex_attribute!(partial, :a, 4, 4.0)             # complete it ...\n@assert validate_term(MyAttrSum(:a), partial; verbose=false, rng=Xoshiro(1))   # ... and it is valid","title":"Missing Attribute Values (NA)"},{"category":"section","location":"guide/term_interface/#Best-Practices","page":"Term Interface","text":"Implement compute() first: Get the full statistic correct before optimizing change_stat()\nUse changestatcheck(): Always verify consistency before using a term\nAvoid global state: Terms should be pure functions of the network\nKeep structs immutable: Don't use mutable state in term structs\nReturn Float64: Even for integer-valued statistics, return Float64 for type stability\nHandle directed vs undirected: Test your term on both network types","title":"Best Practices"},{"category":"section","location":"guide/templates/#Templates-and-Examples","page":"Templates and Examples","text":"ERGMUserterms.jl provides five complete example terms that serve as templates for building your own. Each demonstrates a different pattern for custom term development.","title":"Templates and Examples"},{"category":"section","location":"guide/templates/#Template-Overview","page":"Templates and Examples","text":"Template Pattern Key Feature\nExampleTerm Simple structural No parameters, basic edge iteration\nTemplateTerm Parameterized Generic type parameter, keyword constructor\nWeightedEdges Edge attributes Accessing edge-level covariates\nDyadCovTerm Dyadic covariate Using an external matrix\nInteractionTerm Node interactions Combining multiple vertex attributes","title":"Template Overview"},{"category":"section","location":"guide/templates/#Package-Template:-examples/MyTermPackage","page":"Templates and Examples","text":"The five terms above are term templates. For shipping terms in a package of your own, the repository also carries a package template — examples/MyTermPackage/ — a copyable skeleton (Project.toml, module, tests) whose term ReciprocatedHomophily declares the full set of ERGM.jl traits:\n\n<!– skip-check –>\n\nERGM.required_vertex_attributes(t::ReciprocatedHomophily) = (t.attr,)\nERGM.requires_directed(::ReciprocatedHomophily)           = true\nERGM.is_dyad_dependent(::ReciprocatedHomophily)           = true\nNetworks.supports_missing(::ReciprocatedHomophily)        = true\n\nand, on the strength of those declarations, is validated by validate_term and accepted (or correctly rejected — on an undirected network, or one lacking the attribute) by ERGMModel construction, exactly like an ERGM.jl built-in. See Declaring Your Term's Traits.","title":"Package Template: examples/MyTermPackage"},{"category":"section","location":"guide/templates/#ExampleTerm","page":"Templates and Examples","text":"The simplest possible custom term. Counts edges weighted by vertex ID sum.","title":"ExampleTerm"},{"category":"section","location":"guide/templates/#Implementation","page":"Templates and Examples","text":"The template implementations below re-define local copies of the bundled terms so you can adapt them:\n\nusing ERGM, ERGMUserterms, Networks, Random\nusing Graphs: src, dst                     # edge endpoints are Graphs.jl's\nimport ERGM: name, compute, change_stat    # the shared generics\n\nstruct ExampleTerm <: AbstractUserTerm end\n\nname(::ExampleTerm) = \"example\"\n\nfunction compute(::ExampleTerm, net)\n    total = 0.0\n    for e in edges(net)\n        total += src(e) + dst(e)\n    end\n    return total\nend\n\nfunction change_stat(::ExampleTerm, net, i::Int, j::Int)\n    # Add-direction: adding edge (i,j) adds i + j, whatever the current state\n    return Float64(i + j)\nend\n\n# Covariate-only: opt out of ERGM.jl's conservative dyad-dependent fallback\nERGM.is_dyad_dependent(::ExampleTerm) = false","title":"Implementation"},{"category":"section","location":"guide/templates/#Usage","page":"Templates and Examples","text":"term = ExampleTerm()\nnet = network(5; directed=true)\nadd_edge!(net, 1, 2)\nadd_edge!(net, 3, 4)\n\ncompute(term, net)  # (1+2) + (3+4) = 10.0\nchange_stat(term, net, 2, 3)  # +(2+3) = 5.0\nchange_stat(term, net, 1, 2)  # +(1+2) = 3.0 (state-independent add-direction)\n@assert compute(term, net) == 10.0\n@assert validate_term(term, net; verbose=false, rng=Xoshiro(1))","title":"Usage"},{"category":"section","location":"guide/templates/#When-to-Use-This-Pattern","page":"Templates and Examples","text":"Structural terms with no parameters\nTerms that iterate over all edges\nTerms where the change statistic is a simple local computation","title":"When to Use This Pattern"},{"category":"section","location":"guide/templates/#Adapting-This-Template","page":"Templates and Examples","text":"# Example: Count edges where source ID > target ID\nstruct DescendingEdges <: AbstractUserTerm end\n\nname(::DescendingEdges) = \"descending\"\n\nfunction compute(::DescendingEdges, net)\n    count = 0.0\n    for e in edges(net)\n        src(e) > dst(e) && (count += 1.0)\n    end\n    return count\nend\n\nfunction change_stat(::DescendingEdges, net, i::Int, j::Int)\n    # Add-direction: 1 if this edge would contribute, else 0\n    return i > j ? 1.0 : 0.0\nend\nERGM.is_dyad_dependent(::DescendingEdges) = false\n\n@assert validate_term(DescendingEdges(), net; verbose=false, rng=Xoshiro(1))","title":"Adapting This Template"},{"category":"section","location":"guide/templates/#TemplateTerm","page":"Templates and Examples","text":"A parameterized template with a generic type parameter. Copy and modify for terms that need configuration.","title":"TemplateTerm"},{"category":"section","location":"guide/templates/#Implementation-2","page":"Templates and Examples","text":"struct TemplateTerm{T} <: AbstractUserTerm\n    param::T\n    attr::Symbol\n\n    TemplateTerm(param::T; attr::Symbol=:none) where T = new{T}(param, attr)\nend\n\nname(t::TemplateTerm) = \"template.$(t.param)\"\n\nfunction compute(t::TemplateTerm, net)\n    # Count edges multiplied by parameter\n    return Float64(ne(net)) * t.param\nend\n\nfunction change_stat(t::TemplateTerm, net, i::Int, j::Int)\n    # Add-direction: adding edge (i,j) increases the statistic by the\n    # parameter (state-independent; removals are handled by the sampler)\n    return Float64(t.param)\nend\n\n# Covariate-only term: opt out of ERGM.jl's conservative dyad-dependent\n# fallback (omit this line for terms that read the state of other dyads)\nERGM.is_dyad_dependent(::TemplateTerm) = false","title":"Implementation"},{"category":"section","location":"guide/templates/#Usage-2","page":"Templates and Examples","text":"# Create with different parameter types\nterm_float = TemplateTerm(2.5)\nterm_int = TemplateTerm(3)\n\nnet = network(5; directed=true)\nadd_edge!(net, 1, 2)\nadd_edge!(net, 2, 3)\n\ncompute(term_float, net)  # 2 * 2.5 = 5.0\nchange_stat(term_float, net, 3, 4)  # +2.5 (edge absent)\nchange_stat(term_float, net, 1, 2)  # +2.5 (edge present: same add-direction value)\n@assert validate_term(term_float, net; verbose=false, rng=Xoshiro(1))","title":"Usage"},{"category":"section","location":"guide/templates/#When-to-Use-This-Pattern-2","page":"Templates and Examples","text":"Terms with a scalar parameter\nTerms where the parameter scales the statistic linearly\nPrototype terms before implementing efficient change_stat()","title":"When to Use This Pattern"},{"category":"section","location":"guide/templates/#Adapting-This-Template-2","page":"Templates and Examples","text":"# Example: Weighted edge count with decay\nstruct DecayEdges <: AbstractUserTerm\n    decay::Float64\n    DecayEdges(; decay::Float64=0.5) = new(decay)\nend\n\nname(t::DecayEdges) = \"decay_edges.$(t.decay)\"\n\nfunction compute(t::DecayEdges, net)\n    total = 0.0\n    for e in edges(net)\n        # Weight by position in vertex ordering\n        total += t.decay ^ abs(src(e) - dst(e))\n    end\n    return total\nend\n\nfunction change_stat(t::DecayEdges, net, i::Int, j::Int)\n    return t.decay ^ abs(i - j)\nend\nERGM.is_dyad_dependent(::DecayEdges) = false\n\n@assert validate_term(DecayEdges(), net; verbose=false, rng=Xoshiro(1))","title":"Adapting This Template"},{"category":"section","location":"guide/templates/#WeightedEdges","page":"Templates and Examples","text":"Demonstrates accessing edge attributes from the network.","title":"WeightedEdges"},{"category":"section","location":"guide/templates/#Implementation-3","page":"Templates and Examples","text":"struct WeightedEdges <: AbstractUserTerm\n    attr::Symbol\n    default::Float64\n    WeightedEdges(attr::Symbol=:weight; default::Float64=1.0) = new(attr, default)\nend\n\nname(t::WeightedEdges) = \"weightedges.$(t.attr)\"\n\n# The weight edge (i,j) carries: its stored value, else the default a fresh\n# edge would get. The per-edge getter canonicalises the key on undirected\n# networks ((min,max)) and returns `nothing` for an edge — or a network —\n# without the attribute; the `::Float64` assertion keeps the term type-stable\n# and allocation-free (the attribute store is an untyped `Dict{…,Any}`, and a\n# bare `get` from it would infer `Any` and box on every sampler step)\nfunction _edge_weight(t::WeightedEdges, net, i::Int, j::Int)\n    w = get_edge_attribute(net, t.attr, i, j)\n    return w === nothing ? t.default : Float64(w)::Float64\nend\n\nfunction compute(t::WeightedEdges, net)\n    # Sum over the network's edges with a default — this keeps compute\n    # consistent with change_stat when the sampler adds unweighted edges\n    total = 0.0\n    for e in edges(net)\n        total += _edge_weight(t, net, Int(src(e)), Int(dst(e)))\n    end\n    return total\nend\n\nfunction change_stat(t::WeightedEdges, net, i::Int, j::Int)\n    # Add-direction: the weight edge (i,j) carries, whatever the dyad's state\n    return _edge_weight(t, net, i, j)\nend\nERGM.is_dyad_dependent(::WeightedEdges) = false\n# Deliberately NO `ERGM.required_edge_attributes`: edges without the\n# attribute count at the default, so the term is well defined without it","title":"Implementation"},{"category":"section","location":"guide/templates/#Usage-3","page":"Templates and Examples","text":"net = network(5; directed=true)\nadd_edge!(net, 1, 2)\nadd_edge!(net, 2, 3)\nset_edge_attribute!(net, :weight, 1, 2, 3.0)\nset_edge_attribute!(net, :weight, 2, 3, 1.5)\n\nterm = WeightedEdges(:weight)\ncompute(term, net)  # 3.0 + 1.5 = 4.5\n@assert compute(term, net) == 4.5\n@assert validate_term(term, net; verbose=false, rng=Xoshiro(1))","title":"Usage"},{"category":"section","location":"guide/templates/#When-to-Use-This-Pattern-3","page":"Templates and Examples","text":"Terms that use edge-level data (weights, types, timestamps)\nTerms with a configurable attribute name\nTerms that fall back to a default when attributes are missing","title":"When to Use This Pattern"},{"category":"section","location":"guide/templates/#Adapting-This-Template-3","page":"Templates and Examples","text":"# Example: Sum of squared edge weights\nstruct SquaredWeights <: AbstractUserTerm\n    attr::Symbol\n    SquaredWeights(attr::Symbol=:weight) = new(attr)\nend\n\nname(t::SquaredWeights) = \"sqweights.$(t.attr)\"\n\nfunction _weight(t::SquaredWeights, net, i::Int, j::Int)\n    w = get_edge_attribute(net, t.attr, i, j)\n    return w === nothing ? 1.0 : Float64(w)::Float64\nend\n\nfunction compute(t::SquaredWeights, net)\n    total = 0.0\n    for e in edges(net)\n        total += _weight(t, net, Int(src(e)), Int(dst(e)))^2\n    end\n    return total\nend\n\nfunction change_stat(t::SquaredWeights, net, i::Int, j::Int)\n    return _weight(t, net, i, j)^2\nend\nERGM.is_dyad_dependent(::SquaredWeights) = false\n\n@assert validate_term(SquaredWeights(), net; verbose=false, rng=Xoshiro(1))","title":"Adapting This Template"},{"category":"section","location":"guide/templates/#DyadCovTerm","page":"Templates and Examples","text":"Demonstrates using an external matrix as a dyadic covariate.","title":"DyadCovTerm"},{"category":"section","location":"guide/templates/#Implementation-4","page":"Templates and Examples","text":"struct DyadCovTerm <: AbstractUserTerm\n    covariate::Matrix{Float64}\nend\n\nname(::DyadCovTerm) = \"dyadcov\"\n\nfunction compute(t::DyadCovTerm, net)\n    total = 0.0\n    for e in edges(net)\n        i, j = src(e), dst(e)\n        if i <= size(t.covariate, 1) && j <= size(t.covariate, 2)\n            total += t.covariate[i, j]\n        end\n    end\n    return total\nend\n\nfunction change_stat(t::DyadCovTerm, net, i::Int, j::Int)\n    # Undirected edges are stored canonically as (min, max); read the\n    # covariate the same way compute() sees the edge\n    if !is_directed(net)\n        i, j = minmax(i, j)\n    end\n    i <= size(t.covariate, 1) && j <= size(t.covariate, 2) || return 0.0\n    return t.covariate[i, j]\nend\nERGM.is_dyad_dependent(::DyadCovTerm) = false","title":"Implementation"},{"category":"section","location":"guide/templates/#Usage-4","page":"Templates and Examples","text":"# Distance matrix between actors\ndistance = [\n    0.0  1.0  2.0  3.0\n    1.0  0.0  1.5  2.5\n    2.0  1.5  0.0  1.0\n    3.0  2.5  1.0  0.0\n]\n\nnet = network(4; directed=true)\nadd_edge!(net, 1, 2)\nadd_edge!(net, 3, 4)\n\nterm = DyadCovTerm(distance)\ncompute(term, net)  # distance[1,2] + distance[3,4] = 1.0 + 1.0 = 2.0\nchange_stat(term, net, 1, 3)  # +distance[1,3] = 2.0 (adding edge)\n@assert compute(term, net) == 2.0\n@assert validate_term(term, net; verbose=false, rng=Xoshiro(1))","title":"Usage"},{"category":"section","location":"guide/templates/#When-to-Use-This-Pattern-4","page":"Templates and Examples","text":"Geographic distance effects\nPre-computed similarity scores\nAny dyad-level covariate stored as a matrix","title":"When to Use This Pattern"},{"category":"section","location":"guide/templates/#Adapting-This-Template-4","page":"Templates and Examples","text":"# Example: Binary covariate (same group membership)\nstruct SameGroup <: AbstractUserTerm\n    membership::Vector{Int}\nend\n\nname(::SameGroup) = \"samegroup\"\n\nfunction compute(t::SameGroup, net)\n    count = 0.0\n    for e in edges(net)\n        i, j = src(e), dst(e)\n        i <= length(t.membership) && j <= length(t.membership) || continue\n        t.membership[i] == t.membership[j] && (count += 1.0)\n    end\n    return count\nend\n\nfunction change_stat(t::SameGroup, net, i::Int, j::Int)\n    i <= length(t.membership) && j <= length(t.membership) || return 0.0\n    return t.membership[i] == t.membership[j] ? 1.0 : 0.0\nend\nERGM.is_dyad_dependent(::SameGroup) = false\n\n@assert validate_term(SameGroup([1, 1, 2, 2]), net; verbose=false, rng=Xoshiro(1))","title":"Adapting This Template"},{"category":"section","location":"guide/templates/#InteractionTerm","page":"Templates and Examples","text":"Demonstrates combining multiple vertex attributes into an interaction effect.","title":"InteractionTerm"},{"category":"section","location":"guide/templates/#Implementation-5","page":"Templates and Examples","text":"struct InteractionTerm <: AbstractUserTerm\n    attr1::Symbol\n    attr2::Symbol\nend\n\nname(t::InteractionTerm) = \"interact.$(t.attr1).$(t.attr2)\"\n\n# One vertex's value, 0.0 when it has none. `get_vertex_attribute(net, attr,\n# v)` returns `nothing` when the attribute is absent or the vertex has no\n# value; the zero-fill is reached only by raw compute calls, because the\n# term DECLARES both attributes (bottom of this block) and an ERGMModel\n# refuses a network on which either is absent or set on only some vertices\n# — statnet refuses NA. The `::Float64` assertion keeps the arithmetic\n# type-stable and allocation-free (the store is an untyped `Dict{Int,Any}`).\nfunction _vertex_value(net, attr::Symbol, v::Int)\n    x = get_vertex_attribute(net, attr, v)\n    return x === nothing ? 0.0 : Float64(x)::Float64\nend\n\n# The per-edge contribution of (i,j): a_i b_j + a_j b_i\n_interaction(t::InteractionTerm, net, i::Int, j::Int) =\n    _vertex_value(net, t.attr1, i) * _vertex_value(net, t.attr2, j) +\n    _vertex_value(net, t.attr1, j) * _vertex_value(net, t.attr2, i)\n\nfunction compute(t::InteractionTerm, net)\n    total = 0.0\n    for e in edges(net)\n        total += _interaction(t, net, Int(src(e)), Int(dst(e)))\n    end\n    return total\nend\n\nfunction change_stat(t::InteractionTerm, net, i::Int, j::Int)\n    # Add-direction: the per-edge contribution of (i,j)\n    return _interaction(t, net, i, j)\nend\n\n# The term is meaningless without its two attributes: declare them, and\n# ERGMModel construction raises an ArgumentError naming a missing one\nERGM.required_vertex_attributes(t::InteractionTerm) = (t.attr1, t.attr2)\nERGM.is_dyad_dependent(::InteractionTerm) = false","title":"Implementation"},{"category":"section","location":"guide/templates/#Usage-5","page":"Templates and Examples","text":"net = network(4; directed=true)\nadd_edge!(net, 1, 2)\n\n# Set vertex attributes — on EVERY vertex: a declared attribute with a value\n# on only some vertices is refused by ERGMModel (statnet refuses NA)\nset_vertex_attribute!(net, :age, Dict(1 => 25.0, 2 => 30.0, 3 => 41.0, 4 => 52.0))\nset_vertex_attribute!(net, :income, Dict(1 => 50000.0, 2 => 60000.0, 3 => 55000.0, 4 => 70000.0))\n\nterm = InteractionTerm(:age, :income)\ncompute(term, net)\n# = age[1]*income[2] + age[2]*income[1]\n# = 25*60000 + 30*50000 = 3000000.0\n@assert compute(term, net) == 3000000.0\n@assert validate_term(term, net; verbose=false, rng=Xoshiro(1))\n\n# Misspell an attribute and the model, not the fit, tells you\nerr = try; ERGMModel(ERGMFormula([InteractionTerm(:age, :incom)]), net); nothing; catch e; e; end\n@assert err isa ArgumentError && occursin(\":incom\", err.msg)","title":"Usage"},{"category":"section","location":"guide/templates/#When-to-Use-This-Pattern-5","page":"Templates and Examples","text":"Cross-attribute effects (e.g., age × status)\nSender-receiver attribute interactions\nMulti-dimensional homophily models","title":"When to Use This Pattern"},{"category":"section","location":"guide/templates/#Adapting-This-Template-5","page":"Templates and Examples","text":"# Example: Absolute difference interaction\nstruct DiffInteraction <: AbstractUserTerm\n    attr1::Symbol\n    attr2::Symbol\nend\n\nname(t::DiffInteraction) = \"diffinteract.$(t.attr1).$(t.attr2)\"\n\n# Per-vertex read: 0 B and infers Float64 (the whole-Dict getter would\n# allocate and infer Any on every sampler step — see the Benchmarking guide)\nfunction _value(net, attr::Symbol, v::Int)\n    x = get_vertex_attribute(net, attr, v)\n    return x === nothing ? 0.0 : Float64(x)::Float64\nend\n\n# The per-edge contribution of (i, j): |Δattr1| · |Δattr2|\n_diff_interaction(t::DiffInteraction, net, i::Int, j::Int) =\n    abs(_value(net, t.attr1, i) - _value(net, t.attr1, j)) *\n    abs(_value(net, t.attr2, i) - _value(net, t.attr2, j))\n\nfunction compute(t::DiffInteraction, net)\n    total = 0.0\n    for e in edges(net)\n        total += _diff_interaction(t, net, Int(src(e)), Int(dst(e)))\n    end\n    return total\nend\n\nchange_stat(t::DiffInteraction, net, i::Int, j::Int) = _diff_interaction(t, net, i, j)\nERGM.required_vertex_attributes(t::DiffInteraction) = (t.attr1, t.attr2)\nERGM.is_dyad_dependent(::DiffInteraction) = false\n\n@assert validate_term(DiffInteraction(:age, :income), net; verbose=false, rng=Xoshiro(1))","title":"Adapting This Template"},{"category":"section","location":"guide/templates/#Common-Patterns-Summary","page":"Templates and Examples","text":"","title":"Common Patterns Summary"},{"category":"section","location":"guide/templates/#Counting-Subgraphs","page":"Templates and Examples","text":"For terms that count specific subgraph patterns:\n\n<!– skip-check –>\n\nfunction compute(::TriangleTerm, net)\n    count = 0\n    for i in vertices(net)\n        for j in outneighbors(net, i)\n            for k in outneighbors(net, j)\n                k > i && has_edge(net, i, k) && (count += 1)\n            end\n        end\n    end\n    return count / 3  # Each triangle counted 3 times\nend","title":"Counting Subgraphs"},{"category":"section","location":"guide/templates/#Using-Attributes","page":"Templates and Examples","text":"For terms based on vertex properties, declare the attribute (ERGM.required_vertex_attributes(t::NodeMatchTerm) = (t.attr,)). Inside an ERGMModel a declared vertex attribute is then guaranteed to have a value on every vertex — ERGM._validate_formula refuses a network on which it is absent or partial, with term '…' needs vertex attribute :attr on every vertex … statnet refuses NA — so the get(attrs, v, nothing) fallback below is reached only by raw compute calls. An undeclared attribute read is a validate_traits failure, because on a network lacking the attribute the term would silently become an all-zero column (see Missing Attribute Values (NA)):\n\n<!– skip-check –>\n\nfunction compute(t::NodeMatchTerm, net)\n    # get_vertex_attribute returns an empty Dict when the attribute is\n    # absent; the `nothing` fallback only matters for raw compute calls —\n    # a declared attribute is complete inside a model\n    attrs = get_vertex_attribute(net, t.attr)\n    count = 0\n    for e in edges(net)\n        get(attrs, src(e), nothing) == get(attrs, dst(e), nothing) && (count += 1)\n    end\n    return Float64(count)\nend\nERGM.required_vertex_attributes(t::NodeMatchTerm) = (t.attr,)","title":"Using Attributes"},{"category":"section","location":"guide/templates/#Conditional-Statistics","page":"Templates and Examples","text":"For terms with conditional logic:\n\n<!– skip-check –>\n\nfunction compute(t::ConditionalTerm, net)\n    total = 0.0\n    for e in edges(net)\n        if meets_condition(net, src(e), dst(e), t.threshold)\n            total += contribution(net, src(e), dst(e))\n        end\n    end\n    return total\nend","title":"Conditional Statistics"},{"category":"section","location":"guide/templates/#Choosing-a-Template","page":"Templates and Examples","text":"Your Term Needs Start With\nNo parameters, simple counting ExampleTerm\nA tunable parameter TemplateTerm\nEdge-level data WeightedEdges\nAn external covariate matrix DyadCovTerm\nMultiple vertex attributes InteractionTerm","title":"Choosing a Template"},{"category":"section","location":"guide/validation/#Validation-and-Testing","page":"Validation and Testing","text":"ERGMUserterms.jl provides a comprehensive validation framework to ensure custom terms are correctly implemented before use in ERGM estimation. Incorrect terms lead to biased estimates, so validation is critical.","title":"Validation and Testing"},{"category":"section","location":"guide/validation/#Overview","page":"Validation and Testing","text":"The validation process has three levels:\n\nQuick validation: validate_term() checks all methods work and are consistent\nConsistency checking: change_stat_check() and consistency_check() verify the fundamental relationship\nComprehensive testing: test_term() runs all checks on multiple network types\n\nEvery level draws its random dyads from one rng::AbstractRNG keyword (default Random.default_rng()) and nothing else, so two calls with equal rngs visit the same dyads and return the same verdict at any thread count — and every failure message ends with a literal you can pass back to replay it (see Reproducing a failure).","title":"Overview"},{"category":"section","location":"guide/validation/#validate_term","page":"Validation and Testing","text":"The primary validation function that checks all aspects of a term implementation:\n\n<!– skip-check –>\n\nvalid = validate_term(term, net; verbose=true, traits=true, n_tests=10,\n                      rng=Random.default_rng())","title":"validate_term"},{"category":"section","location":"guide/validation/#What-It-Checks","page":"Validation and Testing","text":"Check Description Failure Means\nname() exists Returns a non-empty string Method not implemented\nname() non-empty String length > 0 Empty name will cause display issues\ncompute() works Returns without error Bug in compute implementation\ncompute() returns Real Type check on return value Wrong return type\nchange_stat() works Returns without error for n_tests random dyads Bug in change_stat implementation\nchange_stat() returns Real Type check on return value Wrong return type\nConsistency changestat matches compute differences on `ntests` random dyads Incorrect change_stat logic\nTrait declarations validate_traits (unless traits=false) A declaration ERGM.jl would act on is wrong\n\nA check that could not run is never reported as ✓. In particular, an attribute that is constant on net cannot be perturbed, so whether the term reads it is undecidable there: validate_traits warns (attribute :a is constant on this network, so whether the term reads it is undecidable; validate on a network where it varies) and drops the ✓ line, and for an attribute the term declares the run fails — validate on a network where the attribute varies.","title":"What It Checks"},{"category":"section","location":"guide/validation/#Parameters","page":"Validation and Testing","text":"Parameter Description Default\nverbose Log each check and each failure true\ntraits Also run validate_traits true\nn_tests Random dyads for the change_stat checks (at least 1; capped at the number of dyads) 10\nrng Source of every random draw; pass a Xoshiro(seed) for a reproducible run Random.default_rng()\n\nnet must be a network ERGM.jl can fit — at least 2 vertices, one-mode, no self-loop — or it is refused with an ArgumentError before any check runs; see Networks the harness refuses. If net has masked dyads and the term does not declare Networks.supports_missing, the checks run on their face value (that is what the term computes; ERGM.jl applies the missing= policy at estimation time) and, with verbose=true, one [ Info: net has k masked dyads; the term declares supports_missing = false, so they are validated at their face value …] line says so — the same line appears on change_stat_check and on consistency_check(…; verbose=true).","title":"Parameters"},{"category":"section","location":"guide/validation/#Worked-Example","page":"Validation and Testing","text":"using ERGM, ERGMUserterms, Networks, Random\n\nterm = ExampleTerm()\nnet = network(10; directed=true)\nfor (i,j) in [(1,2),(2,3),(3,1),(1,4),(4,5)]\n    add_edge!(net, i, j)\nend\n\nvalid = validate_term(term, net; rng=Xoshiro(1))\n# [ Info: ✓ name() returns: example\n# [ Info: ✓ compute() returns: 26.0 (Float64)\n# [ Info: Testing change_stat() with 10 random dyads...\n# [ Info: ✓ change_stat() returns valid values\n# [ Info: Running consistency check...\n# [ Info: ✓ change_stat() consistent with compute()\n# [ Info: ✓ declared attributes match the ones the term reads\n# [ Info: ✓ is_dyad_dependent = false holds under toggling of other dyads\n# [ Info: ✓ accepted by ERGMModel construction\n# true\n@assert valid","title":"Worked Example"},{"category":"section","location":"guide/validation/#Silent-Validation","page":"Validation and Testing","text":"For programmatic use, run silently and check the returned flag — with verbose=false nothing is logged, but the flag is still the full verdict:\n\nif !validate_term(term, net; verbose=false, rng=Xoshiro(1))\n    error(\"Term validation failed!\")\nend","title":"Silent Validation"},{"category":"section","location":"guide/validation/#Early-Termination","page":"Validation and Testing","text":"The interface checks all run, whatever fails: name(), compute() and the change_stat() type check on n_tests random dyads each record their own failure (with verbose=true, one warning each), so one run shows every broken method. The two expensive stages are gated on everything before them having passed:\n\nthe consistency check (change_stat against brute-force compute differences) runs only if name, compute and the change_stat type check all passed — a change_stat that errors or returns a non-Real cannot be compared, and a compute that fails has nothing to compare it to;\nthe trait checks (validate_traits, unless traits=false) run only if the whole interface passed — the declarations of a term whose statistic is wrong are not worth exercising.\n\nverbose=false changes only the logging; the returned flag is the same.","title":"Early Termination"},{"category":"section","location":"guide/validation/#change*stat*check","page":"Validation and Testing","text":"Focused check that change_stat() correctly predicts differences in compute():\n\nconsistent = change_stat_check(term, net; n_tests=10, verbose=true, tol=1e-10,\n                               rng=Random.default_rng())\n@assert consistent","title":"changestatcheck"},{"category":"section","location":"guide/validation/#How-It-Works","page":"Validation and Testing","text":"For each of n_tests random dyads (i,j) drawn from rng (never i == j; on a copy of the network):\n\nGet predicted = change_stat(term, net, i, j)\nCompute expected = compute(term, net⁺ij) − compute(term, net⁻ij) by forcing the edge present/absent (the dyad's edge attributes are snapshotted and restored across the toggles)\nCheck |predicted − expected| < tol\nState-independence: toggle the dyad and call change_stat(term, net, i, j) again — the value must not change (toggle-direction terms fail here)","title":"How It Works"},{"category":"section","location":"guide/validation/#Parameters-2","page":"Validation and Testing","text":"Parameter Description Default\nn_tests Number of random dyads to test (at least 1: a run that checks no dyad returns no verdict) 10\nverbose Print details of any failures true\ntol Numerical tolerance 1e-10\nrng Source of every random draw; pass a Xoshiro(seed) for a reproducible run Random.default_rng()\n\nEvery failure names the dyad and ends with the seed line reproduce with rng=Xoshiro(0x…, 0x…, 0x…, 0x…) passed to change_stat_check.","title":"Parameters"},{"category":"section","location":"guide/validation/#Diagnosing-Failures","page":"Validation and Testing","text":"When inconsistencies are found, verbose output shows the details:\n\n<!– skip-check –>\n\nchange_stat_check(broken_term, net; verbose=true)\n# [ Warning: Inconsistency at dyad (3,7): change_stat=2.0, brute-force=1.0;\n#            reproduce with rng=Xoshiro(0x…, 0x…, 0x…, 0x…) passed to change_stat_check\n# false\n\nToggle-direction terms (the pre-0.2 convention) fail the state-independence step with a dedicated message:\n\n<!– skip-check –>\n\nchange_stat_check(old_convention_term, net; verbose=true)\n# [ Warning: State-dependent change_stat at dyad (2,5): -1.0 with edge state\n#            as-is vs 1.0 after toggling. change_stat must return the\n#            add-direction change regardless of whether the edge exists;\n#            reproduce with rng=Xoshiro(0x…, 0x…, 0x…, 0x…) passed to change_stat_check\n# false","title":"Diagnosing Failures"},{"category":"section","location":"guide/validation/#Reproducing-a-failure","page":"Validation and Testing","text":"The rng=Xoshiro(…) literal at the end of every failure message is the state of the harness's rng when the call started. The harness draws its dyads straight from rng, so passing that literal back to the entry point named in the message replays the identical dyad sequence — the failing dyad included:\n\n<!– skip-check –>\n\nchange_stat_check(broken_term, net; verbose=true,\n                  rng=Xoshiro(0x62bea62705299f9d, 0xb2196eb285598f6a,\n                              0x813136b07248b437, 0x783171c16f41f41d))\n\nThe literal is exact for Xoshiro and for the default task-local rng (which copies to one). For a MersenneTwister no short literal exists, so its failures carry no hint — re-seed the generator you passed. test_term prints its literal in the report header, since the failure may sit inside the random network it generated.\n\nCommon causes of inconsistency:\n\nCause Symptom Fix\nToggle-direction sign flip State-dependence warning; sign follows has_edge Delete the has_edge(net, i, j) ? -delta : delta idiom; always return the add-direction value\nMissing contribution predicted < actual Account for all affected edges/triangles\nDouble counting predicted > actual Avoid counting the same contribution twice\nOff-by-one close but not exact Check edge iteration bounds","title":"Reproducing a failure"},{"category":"section","location":"guide/validation/#Increasing-Test-Coverage","page":"Validation and Testing","text":"# Quick check (default)\nchange_stat_check(term, net; n_tests=10)\n\n# Thorough check\nchange_stat_check(term, net; n_tests=100)\n\n# Very thorough\nchange_stat_check(term, net; n_tests=1000)","title":"Increasing Test Coverage"},{"category":"section","location":"guide/validation/#consistency_check","page":"Validation and Testing","text":"A more thorough consistency check with an optional exhaustive mode:\n\n# Random sampling (default): distinct random dyads drawn from `rng`\nconsistent = consistency_check(term, net; rng=Xoshiro(1))\n@assert consistent\n\n# Exhaustive check of ALL possible edges (no randomness involved)\nconsistent = consistency_check(term, net; exhaustive=true)\n@assert consistent","title":"consistency_check"},{"category":"section","location":"guide/validation/#Random-vs-Exhaustive","page":"Validation and Testing","text":"Mode Edges Checked Speed Completeness\nRandom up to min(100, number of dyads) distinct random dyads (never i == j; (min, max) on an undirected network) Fast Probabilistic\nExhaustive Every dyad once: all n(n−1) ordered pairs on a directed network, all n(n−1)/2 unordered pairs on an undirected one Slow Complete\n\nconsistency_check is silent by default; verbose=true reports the first failing dyad with its replay literal. Both modes stop at the first failure.","title":"Random vs Exhaustive"},{"category":"section","location":"guide/validation/#When-to-Use-Each-Mode","page":"Validation and Testing","text":"# During development: use random for quick iteration\nconsistency_check(term, net)\n\n# Before release: use exhaustive on small networks\nsmall_net = network(10; directed=true)\n# ... add edges ...\nconsistency_check(term, small_net; exhaustive=true)","title":"When to Use Each Mode"},{"category":"section","location":"guide/validation/#Numerical-Tolerance","page":"Validation and Testing","text":"Both checking functions accept a tolerance parameter:\n\n# Default tolerance (suitable for exact computations)\nconsistency_check(term, net; tol=1e-10)\n\n# Relaxed tolerance (for floating-point intensive terms)\nconsistency_check(term, net; tol=1e-6)","title":"Numerical Tolerance"},{"category":"section","location":"guide/validation/#test_term","page":"Validation and Testing","text":"Comprehensive test suite that creates test networks and runs all validation checks:\n\npassed = test_term(term; n_vertices=20, density=0.1, n_tests=100,\n                   rng=Xoshiro(1))\n@assert passed\n\nThe generated networks are directed and attribute-free unless you say otherwise. A term that declares ERGM.required_vertex_attributes needs vertex_attributes=; one that declares ERGM.requires_undirected needs directed=false. Without them validate_term reports the mismatch (term declares required vertex attribute :group, which the validation network does not have / … validated on a directed network; validate it on an undirected one) and the run fails — correctly, but not because the term is wrong:\n\n# The package template's term declares :group and requires_directed\nusing ERGM, ERGMUserterms, Random\ninclude(joinpath(pkgdir(ERGMUserterms), \"examples\", \"MyTermPackage\", \"src\", \"MyTermPackage.jl\"))\nusing .MyTermPackage: ReciprocatedHomophily\n@assert test_term(ReciprocatedHomophily(:group); n_vertices=12, n_tests=20, rng=Xoshiro(1),\n                  vertex_attributes=Dict(:group => (v -> isodd(v) ? \"a\" : \"b\")))\n\n# A term defined on undirected networks only is tested on undirected ones\n@assert test_term(TemplateTerm(2.0); n_vertices=12, n_tests=20, directed=false, rng=Xoshiro(1))","title":"test_term"},{"category":"section","location":"guide/validation/#What-It-Does","page":"Validation and Testing","text":"Creates a random network with specified size and density (edges drawn from rng; directed unless directed=false) and sets the vertex_attributes on it\nRuns full validate_term() on the random network with n_tests random dyads and the same rng\nTests on an empty network (0 edges) of the same kind\nTests on a complete network (all possible edges) of the same kind\nReports results","title":"What It Does"},{"category":"section","location":"guide/validation/#Parameters-3","page":"Validation and Testing","text":"Parameter Description Default\nn_vertices Size of random test network (at least 2 — an ArgumentError otherwise) 20\ndensity Edge density, a proportion of the dyads (0.0 to 1.0; anything else is an ArgumentError) 0.1\nn_tests Random dyads for the change_stat checks (at least 1; passed to validate_term) 100\ndirected Directed (true) or undirected (false) generated networks true\nvertex_attributes attr => spec pairs set on every generated network; spec is a function v -> value or a vector with one value per vertex (length ≥ n_vertices) Dict{Symbol,Any}()\nrng Source of every random draw (the network and the dyads) Random.default_rng()\n\nn_tests is honoured: before 0.2.0 it was accepted and ignored (the network was validated with a hard-wired 10 + 5 dyads); it is now forwarded to validate_term, which caps it at the number of dyads in the generated network. The report header prints the seed line (replay with rng=Xoshiro(0x…, 0x…, 0x…, 0x…)), since a failure may sit inside the random network test_term generated.","title":"Parameters"},{"category":"section","location":"guide/validation/#Example-Output","page":"Validation and Testing","text":"Testing term: example\n(replay with rng=Xoshiro(0x…, 0x…, 0x…, 0x…))\n==================================================\n[ Info: ✓ name() returns: example\n[ Info: ✓ compute() returns: … (Float64)          # depends on the random network\n[ Info: Testing change_stat() with 100 random dyads...\n[ Info: ✓ change_stat() returns valid values\n[ Info: Running consistency check...\n[ Info: ✓ change_stat() consistent with compute()\n[ Info: ✓ declared attributes match the ones the term reads\n[ Info: ✓ is_dyad_dependent = false holds under toggling of other dyads\n[ Info: ✓ accepted by ERGMModel construction\n\nAdditional tests:\n✓ Works on empty network: 0.0\n✓ Works on complete network: 990.0\n==================================================\nAll tests PASSED","title":"Example Output"},{"category":"section","location":"guide/validation/#Testing-Strategy","page":"Validation and Testing","text":"# Step 1: Quick validation during development\nvalidate_term(term, net)\n\n# Step 2: Comprehensive test when implementation is complete\ntest_term(term)\n\n# Step 3: Test with different network sizes (seeded, so a failure replays)\ntest_term(term; n_vertices=5, density=0.3, rng=Xoshiro(1))    # Small dense\ntest_term(term; n_vertices=50, density=0.05, rng=Xoshiro(2))  # Large sparse\ntest_term(term; n_vertices=10, density=0.5, rng=Xoshiro(3))   # Medium moderate\n\n# Step 4: Exhaustive consistency on small network\nrng = Xoshiro(4)\nsmall_net = network(8; directed=true)\nfor _ in 1:15\n    i, j = rand(rng, 1:8), rand(rng, 1:8)\n    i != j && add_edge!(small_net, i, j)\nend\nconsistency_check(term, small_net; exhaustive=true)","title":"Testing Strategy"},{"category":"section","location":"guide/validation/#Debugging-Failed-Validation","page":"Validation and Testing","text":"","title":"Debugging Failed Validation"},{"category":"section","location":"guide/validation/#Step-1:-Identify-the-Failing-Check","page":"Validation and Testing","text":"validate_term(term, net; verbose=true)\n# Watch for ✗ or Warning messages","title":"Step 1: Identify the Failing Check"},{"category":"section","location":"guide/validation/#Step-2:-Test-Methods-Individually","page":"Validation and Testing","text":"# Test name\nprintln(\"name: \", name(term))\n\n# Test compute on a known network\nnet = network(3; directed=true)\nadd_edge!(net, 1, 2)\nprintln(\"compute (1 edge): \", compute(term, net))\n\n# Test change_stat\nprintln(\"change_stat(1,2): \", change_stat(term, net, 1, 2))  # Existing edge\nprintln(\"change_stat(2,3): \", change_stat(term, net, 2, 3))  # Non-existing edge","title":"Step 2: Test Methods Individually"},{"category":"section","location":"guide/validation/#Step-3:-Manual-Consistency-Check","page":"Validation and Testing","text":"# Verify the fundamental relationship manually\nnet = network(4; directed=true)\nadd_edge!(net, 1, 2)\nadd_edge!(net, 2, 3)\n\nbefore = compute(term, net)\ndelta = change_stat(term, net, 1, 3)\n\n# Edge (1,3) is absent, so forcing it present gives the add-direction\n# difference directly\nadd_edge!(net, 1, 3)\nafter = compute(term, net)\n\nprintln(\"Before: $before\")\nprintln(\"After: $after\")\nprintln(\"Predicted delta: $delta\")\nprintln(\"Actual delta: $(after - before)\")\nprintln(\"Match: $(abs(delta - (after - before)) < 1e-10)\")","title":"Step 3: Manual Consistency Check"},{"category":"section","location":"guide/validation/#Step-4:-Check-Edge-Cases","page":"Validation and Testing","text":"# Empty network\nempty_net = network(5)\nprintln(\"Empty: \", compute(term, empty_net))\n\n# Single edge\nsingle_net = network(5; directed=true)\nadd_edge!(single_net, 1, 2)\nprintln(\"Single edge: \", compute(term, single_net))\n\n# Self-referential (if relevant)\nprintln(\"change_stat(1,1): should handle gracefully\")","title":"Step 4: Check Edge Cases"},{"category":"section","location":"guide/validation/#Networks-the-harness-refuses","page":"Validation and Testing","text":"A network ERGM.jl cannot fit is refused up front, with an ArgumentError carrying ERGM.jl's own reasoning, by every entry point that takes a network — validate_term, validate_traits, change_stat_check, consistency_check and benchmark_term — before any dyad is visited. It is never validated dyad by dyad and then rejected at the ERGMModel step in words that blame the term. Three cases:\n\nFewer than 2 vertices. There is no dyad to check, so there is nothing a verdict could rest on. (test_term refuses n_vertices < 2 and a density outside [0, 1] for the same reason, and every validator refuses n_tests < 1 — a run that checks no dyad returns no verdict.)\nTwo-mode networks (below).\nA self-loop present. ERGM.jl models the off-diagonal dyads only: a term's statistics would count a loop, but the pseudo-likelihood, nobs, the MH proposal and every simulation never touch the diagonal, so ERGMModel refuses the network (R ergm warns \"This network contains loops\" here). The harness would otherwise have validated compute counting the loop, every check green:\n\nusing ERGM, ERGMUserterms, Networks, Random\nnl = network(6; directed=true, loops=true)\nadd_edge!(nl, 1, 1); add_edge!(nl, 1, 2)         # a self-loop at vertex 1\nrefused = try\n    validate_term(ExampleTerm(), nl; rng=Xoshiro(1)); false\ncatch e\n    e isa ArgumentError && occursin(\"contains 1 self-loop (at vertex 1)\", e.msg)\nend\n@assert refused\nrem_edge!(nl, 1, 1)                              # the fix the message suggests\n@assert validate_term(ExampleTerm(), nl; verbose=false, rng=Xoshiro(1))\n\n(loops=true is a capability, not a defect: a loops-allowed network with no loop present validates normally.)","title":"Networks the harness refuses"},{"category":"section","location":"guide/validation/#Two-mode-networks","page":"Validation and Testing","text":"ERGM.jl fits one-mode networks only: ERGMModel refuses a two-mode (bipartite-flagged) network — network(n; bipartite=k) or a BipartiteNetwork — because statnet's bipartite terms (b1degree, b2degree, b1factor, b1nodematch, …) and the two-mode proposal kernel are not implemented. The harness refuses one too, up front and with the same message:\n\nusing ERGM, ERGMUserterms, Networks, Random\nbp = network(6; directed=false, bipartite=3)     # modes {1,2,3} and {4,5,6}\nadd_edge!(bp, 1, 4)\n@assert !add_edge!(bp, 1, 2)                     # a within-mode pair cannot hold an edge\nrefused = try\n    validate_term(ExampleTerm(), bp; rng=Xoshiro(1)); false\ncatch e\n    e isa ArgumentError && occursin(\"one-mode networks only\", e.msg)\nend\n@assert refused\n\nThe refusal is not cosmetic. The harness draws and enumerates dyads over every off-diagonal pair; on a two-mode network the within-mode pairs are structurally absent (add_edge! returns false), so the brute-force reference compute(present) − compute(absent) would read 0 there while a correct change_stat returns its value, and a correct term would be reported inconsistent (Inconsistency at dyad (1,3): change_stat=4.0, brute-force=0.0). Validate on a one-mode network (network(n; directed=…) without bipartite=); test_term and profile_term build one-mode, loop-free networks of their own (directed unless directed=false, carrying the vertex_attributes you pass — both take the same two keywords).","title":"Two-mode networks"},{"category":"section","location":"guide/validation/#Validation-against-R","page":"Validation and Testing","text":"The harness compares your change_stat with a brute-force reference (compute with the dyad present minus compute with it absent). That reference is only as good as the toggle-and-recompute code behind it, so it is itself pinned against statnet: test/fixtures/userterms_examples.toml, generated by the checked-in test/fixtures/r/userterms_examples.R (ergm 4.12.0, R 4.6.1, with a [provenance] block so Networks.load_golden accepts it), records summary() and the complete ergmMPLE(output=\"array\") change-statistic array — every ordered dyad — for the statnet terms the bundled terms are mathematically identical to:\n\nERGMUserterms.jl term statnet term\nExampleTerm() nodecov(\"id\"), id = vertex index\nTemplateTerm(1.0) edges\nWeightedEdges() edgecov(W), W = 2.5 on the weighted arcs and 1.0 (the default) elsewhere\nDyadCovTerm(cov) edgecov(cov), asymmetric cov[i,j] = 2i + j\nInteractionTerm(:a, :b) edgecov(M), M[i,j] = a_i b_j + a_j b_i\nReciprocatedHomophily(:group) (examples/MyTermPackage) mutual(same=\"group\", diff=FALSE)\n\nThe network is an 8-vertex directed graph from a literal edge list (the template's own test network plus four arcs, so every change statistic takes more than one value) and its undirected projection, on which W and cov are symmetrised to their (min, max) entry — the canonical key WeightedEdges/DyadCovTerm read on an undirected network, which is how that branch gets an R number too. The testset \"Golden: bundled terms and the harness match statnet\" compares, at the fixture's 1e-9 (deterministic integer/rational arithmetic on a fixed graph — no estimator, no Monte Carlo, so any disagreement is a term bug), each term's compute, each term's change_stat on every dyad (in both dyad orders when undirected), and ERGMUserterms._brute_change_stat on every dyad. R's coefficient labels (nodecov.id, edgecov.W, …) are recorded in the fixture but deliberately not claimed: a user term keeps the name its author gives it.\n\nRegenerate the fixture from the package root with Rscript test/fixtures/r/userterms_examples.R > test/fixtures/userterms_examples.toml (needs ergm installed in R).","title":"Validation against R"},{"category":"section","location":"guide/validation/#Best-Practices","page":"Validation and Testing","text":"Validate early and often: Run validate_term() after each change to compute() or change_stat()\nStart with small networks: Use 3-5 node networks where you can verify results by hand\nTest both edge states: Verify change_stat for both existing and non-existing edges\nUse exhaustive checks: Run consistency_check(exhaustive=true) on small networks before deployment\nCheck numerical precision: Use appropriate tolerance for floating-point computations\nTest multiple densities: Validate on sparse, moderate, and dense networks\nDocument expected values: For small test cases, compute expected statistics by hand\nAutomate testing: Include validation in your test suite, with an explicit rng=Xoshiro(seed) so a red run is the same run every time","title":"Best Practices"},{"category":"section","location":"api/utilities/#Utilities-API-Reference","page":"Utilities","text":"This page documents the documentation helper functions and utility tools.","title":"Utilities API Reference"},{"category":"section","location":"api/utilities/#Documentation-Helpers","page":"Utilities","text":"","title":"Documentation Helpers"},{"category":"function","location":"api/utilities/#ERGMUserterms.term_signature","page":"Utilities","text":"term_signature(term::AbstractERGMTerm) -> String\n\nGenerate a signature string for a term showing its fields and types.\n\nExample\n\nusing ERGM, ERGMUserterms\nterm_signature(TemplateTerm(2.5))        # \"TemplateTerm(param::Float64, attr::Symbol)\"\nterm_signature(ExampleTerm())            # \"ExampleTerm\"\n\n\n\n\n\n","title":"ERGMUserterms.term_signature"},{"category":"function","location":"api/utilities/#ERGMUserterms.term_documentation","page":"Utilities","text":"term_documentation(term::AbstractERGMTerm) -> String\n\nGenerate a Markdown documentation stub for a term: heading, signature (term_signature), name, one line per field with its current value, and the three interface methods.\n\nExample\n\nusing ERGM, ERGMUserterms\ndoc = term_documentation(TemplateTerm(2.5))\noccursin(\"Name: template.2.5\", doc)                     # true\noccursin(\"`param::Float64` = 2.5\", doc)                 # true\n\n\n\n\n\n","title":"ERGMUserterms.term_documentation"},{"category":"section","location":"api/validation/#Validation-API-Reference","page":"Validation","text":"This page documents the validation, testing, and consistency checking functions.","title":"Validation API Reference"},{"category":"section","location":"api/validation/#Term-Validation","page":"Validation","text":"","title":"Term Validation"},{"category":"section","location":"api/validation/#Consistency-Checking","page":"Validation","text":"","title":"Consistency Checking"},{"category":"section","location":"api/validation/#Benchmarking","page":"Validation","text":"","title":"Benchmarking"},{"category":"section","location":"api/validation/#Term-Macro","page":"Validation","text":"","title":"Term Macro"},{"category":"function","location":"api/validation/#ERGMUserterms.validate_term","page":"Validation","text":"validate_term(term::AbstractERGMTerm, net::Network; verbose=true, traits=true,\n              n_tests=10, rng=Random.default_rng()) -> Bool\n\nValidate that a term is correctly implemented.\n\nChecks the method interface:\n\ncompute() returns a Real\nchange_stat() returns a Real on n_tests random dyads\nname() returns a non-empty string\nChange statistics are consistent with compute differences (add-direction convention, change_stat_check on n_tests random dyads)\n\nand, unless traits=false, the term's trait declarations — see validate_traits for exactly what is asserted about ERGM.required_vertex_attributes, ERGM.required_edge_attributes, ERGM.requires_directed/requires_undirected, ERGM.is_dyad_dependent and Networks.supports_missing. The trait checks also assert that the term is accepted by ERGMModel construction on net, so a term that passes validate_term is a term ERGM.jl will fit.\n\nnet must carry every vertex/edge attribute the term declares (validate against a network the term is meant for), with a value on every vertex — ERGM.jl refuses partial attributes at model construction, and so does this — and with values that vary across vertices (a constant declared attribute cannot be shown to be read, and the run fails). A network ERGM.jl cannot fit is refused up front with an ArgumentError rather than validated: fewer than 2 vertices (no dyad to check), a two-mode network, or one containing a self-loop. A verdict is never returned without evidence: n_tests must be at least 1.\n\nKeywords\n\nverbose=true: log each check (@info) and each failure (@warn).\ntraits=true: run validate_traits after the interface checks.\nn_tests=10: random dyads drawn for the change_stat checks (at least 1; capped at the number of dyads in net).\nrng=Random.default_rng(): the source of every random draw. Two calls with equal rngs visit the same dyads. Every failure message ends with reproduce with rng=Xoshiro(0x…, 0x…, 0x…, 0x…), the rng state on entry — pass it back to replay the run.\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nnet = network(10; directed=true)\nfor (i, j) in [(1, 2), (2, 3), (3, 1), (1, 4), (4, 5)]\n    add_edge!(net, i, j)\nend\nvalidate_term(ExampleTerm(), net; verbose=false, rng=Xoshiro(1))   # true\n\n\n\n\n\n","title":"ERGMUserterms.validate_term"},{"category":"function","location":"api/validation/#ERGMUserterms.validate_traits","page":"Validation","text":"validate_traits(term::AbstractERGMTerm, net::Network; verbose=true,\n                rng=Random.default_rng()) -> Bool\n\nExercise a term's declarations under ERGM.jl's public term-trait protocol (ERGM.required_vertex_attributes, ERGM.required_edge_attributes, ERGM.requires_directed, ERGM.requires_undirected, ERGM.is_dyad_dependent, Networks.supports_missing) against net.\n\nChecked, in order:\n\nAttributes are the ones it reads. Every attribute the term declares must exist on net — a declared vertex attribute with a value on every vertex, since ERGM.jl refuses a partial attribute at model construction the way statnet refuses NA — and must actually move the term's statistic when its values are perturbed (otherwise the declaration is a warning: an over-declaration needlessly narrows the networks the term can be fitted to). Conversely, a term whose statistic moves when an undeclared vertex attribute is perturbed fails: it reads an attribute ERGM.jl will not validate, so on a network lacking that attribute it would silently become an all-zero design column instead of an error. (An undeclared edge attribute is only warned about — terms like WeightedEdges read one but fall back to a default, so they genuinely do not require it.) An attribute that is constant on net cannot be perturbed, so whether the term reads it is undecidable there: the check is reported as not run (never as ✓), and for a declared attribute the run fails — validate on a network where the attribute varies.\nDirection requirements are honoured. A term declaring requires_directed must be rejected by ERGMModel construction on the undirected twin of net (and symmetrically for requires_undirected).\nDyad-independence claims hold. A term declaring is_dyad_dependent(term) == false must return the same change statistic for a dyad no matter how other dyads are toggled. (true, the conservative default, asserts nothing.)\nMissing-data claims hold. A term declaring Networks.supports_missing(term) == true must return the same statistic however the face value of a masked dyad is flipped — that is what it means to honour the mask. (false, the default, asserts nothing: the statistic then counts masked dyads at face value, and the missing-data treatment lives in the estimator.)\n\nFinally, ERGMModel construction with the term on net must succeed (ERGM._validate_formula is what runs there; its message is reported verbatim on failure).\n\nKeywords\n\nverbose=true: log each check (@info) and each failure (@warn).\nrng=Random.default_rng(): the source of every random draw (the source dyads of check 3 and the masked dyads of check 4). Every failure message ends with reproduce with rng=Xoshiro(0x…, …), the rng state on entry.\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nnet = network(6; directed=true)\nset_vertex_attribute!(net, :a, Dict(v => Float64(v) for v in 1:6))\nadd_edge!(net, 1, 2); add_edge!(net, 2, 3)\nvalidate_traits(InteractionTerm(:a, :a), net; verbose=false, rng=Xoshiro(1))  # true\n\n\n\n\n\n","title":"ERGMUserterms.validate_traits"},{"category":"function","location":"api/validation/#ERGMUserterms.test_term","page":"Validation","text":"test_term(term::AbstractERGMTerm; n_vertices=20, density=0.1, n_tests=100,\n          directed=true, vertex_attributes=Dict{Symbol,Any}(),\n          rng=Random.default_rng()) -> Bool\n\nComprehensive test suite for an ERGM term: builds a random network of n_vertices vertices at edge density (directed unless directed=false; edges drawn from rng), gives it the vertex attributes in vertex_attributes, runs validate_term on it with n_tests random dyads and the same rng, then checks compute on an empty and on a complete network of the same kind (same directedness and attributes). Prints a report (including the rng=Xoshiro(0x…, …) literal that replays the whole run) and returns whether everything passed.\n\nThe generated networks carry no vertex attributes unless you ask, and are directed unless you ask: a term that declares ERGM.required_vertex_attributes needs vertex_attributes, one that declares ERGM.requires_undirected needs directed=false — otherwise validate_term (correctly) reports the mismatch and the run FAILS.\n\nKeywords\n\nn_vertices=20, density=0.1: size (at least 2) and edge density (in [0, 1]) of the random network; anything else is an ArgumentError, not a report on a network with no dyad to check.\nn_tests=100: random dyads for the change_stat checks (at least 1; capped at the number of dyads).\ndirected=true: build a directed (true) or undirected (false) network.\nvertex_attributes=Dict{Symbol,Any}(): attr => spec pairs set on every generated network; spec is either a function of the vertex index (v -> value) or a vector with one value per vertex (length ≥ n_vertices).\nrng=Random.default_rng(): the source of every random draw — the network and the dyads.\n\nExample\n\nusing ERGM, ERGMUserterms, Random\ntest_term(ExampleTerm(); n_vertices=8, density=0.2, n_tests=5, rng=Xoshiro(2))   # true\n# A term declaring vertex attributes needs the generated network to carry them\ntest_term(InteractionTerm(:a, :b); n_vertices=8, n_tests=5, rng=Xoshiro(2),\n          vertex_attributes=Dict(:a => (v -> Float64(v)), :b => (v -> Float64(9 - v))))   # true\n# A term defined on undirected networks only (ERGM.requires_undirected) is\n# tested on one\ntest_term(TemplateTerm(2.0); n_vertices=8, n_tests=5, directed=false, rng=Xoshiro(2))   # true\n\n\n\n\n\n","title":"ERGMUserterms.test_term"},{"category":"function","location":"api/validation/#ERGMUserterms.change_stat_check","page":"Validation","text":"change_stat_check(term::AbstractERGMTerm, net::Network; n_tests=10, verbose=true,\n                  tol=1e-10, rng=Random.default_rng()) -> Bool\n\nVerify that change_stat returns the add-direction change statistic required by ERGM.jl: compute with edge (i,j) present minus compute with it absent, independent of the dyad's current state. n_tests random dyads (at least 1; drawn from rng, never i == j) are toggled on a copy of net and restored; each is checked twice — as-is, and with the dyad's state flipped — so a toggle-direction term fails the second check. A network with fewer than 2 vertices, a two-mode one, or one containing a self-loop is refused with an ArgumentError (ERGM.jl cannot fit it; see validate_term).\n\nEvery failure message (verbose=true) names the dyad and ends with reproduce with rng=Xoshiro(0x…, …), the rng state on entry; pass it back to replay the same dyad sequence.\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nnet = network(8; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 2, 3); add_edge!(net, 3, 1)\nchange_stat_check(ExampleTerm(), net; n_tests=5, rng=Xoshiro(1))          # true\nchange_stat_check(ExampleTerm(), net; n_tests=5, rng=Xoshiro(1)) ==\n    change_stat_check(ExampleTerm(), net; n_tests=5, rng=Xoshiro(1))      # true\n\n\n\n\n\n","title":"ERGMUserterms.change_stat_check"},{"category":"function","location":"api/validation/#ERGMUserterms.consistency_check","page":"Validation","text":"consistency_check(term::AbstractERGMTerm, net::Network; exhaustive=false,\n                  tol=1e-10, verbose=false, rng=Random.default_rng()) -> Bool\n\nFull consistency check between compute and the add-direction change_stat (see change_stat_check); stops at the first inconsistent dyad. With exhaustive=true every dyad of net is checked once — each ordered pair on a directed network, each unordered pair (i < j) on an undirected one (can be slow for large networks); otherwise up to min(100, number of dyads) distinct random dyads drawn from rng (never i == j; canonicalised to (min, max) on an undirected network). verbose=true reports the failing dyad, with the rng=Xoshiro(0x…, …) replay literal. A network with fewer than 2 vertices, a two-mode one, or one containing a self-loop is refused with an ArgumentError.\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nnet = network(6; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 2, 3)\nconsistency_check(TemplateTerm(2.0), net; exhaustive=true)           # true\nconsistency_check(TemplateTerm(2.0), net; rng=Xoshiro(3))            # true\n\n\n\n\n\n","title":"ERGMUserterms.consistency_check"},{"category":"function","location":"api/validation/#ERGMUserterms.benchmark_term","page":"Validation","text":"benchmark_term(term::AbstractERGMTerm, net::Network; n_iter=1000,\n               rng=Random.default_rng()) -> NamedTuple\n\nBenchmark compute() and change_stat() performance: n_iter timed calls of each (at least 1), the change_stat calls on random dyads drawn from rng. Returns (compute_mean, compute_std, change_stat_mean, change_stat_std, speedup) in seconds; speedup = compute_mean / change_stat_mean. A network with fewer than 2 vertices, a two-mode one, or one containing a self-loop is refused with an ArgumentError, as by every validator.\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nnet = network(10; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 2, 3)\nb = benchmark_term(ExampleTerm(), net; n_iter=100, rng=Xoshiro(1))\nb.speedup > 0     # true\n\n\n\n\n\n","title":"ERGMUserterms.benchmark_term"},{"category":"function","location":"api/validation/#ERGMUserterms.profile_term","page":"Validation","text":"profile_term(term::AbstractERGMTerm; sizes=[10, 20, 40], density=0.1,\n             n_iter=200, directed=true, vertex_attributes=Dict{Symbol,Any}(),\n             rng=Random.default_rng()) -> Vector{NamedTuple}\n\nProfile a term's compute/change_stat cost across network sizes. Returns one benchmark_term result per size (with n_vertices and the realized edge count ne added), useful for spotting change statistics that do not scale (e.g. O(edges) instead of O(degree)). The random networks and the benchmarked dyads are all drawn from rng.\n\nThe generated networks are built exactly as test_term builds them: directed unless directed=false, and carrying the vertex attributes in vertex_attributes (attr => v -> value or attr => vector). Without them a term declaring ERGM.requires_undirected cannot be profiled at all, and an attribute-declaring term is timed on its attribute-absent fallback branch rather than its real cost.\n\nKeywords\n\nsizes=[10, 20, 40]: vertex counts (each at least 2) of the random networks.\ndensity=0.1: edge density of each random network, in [0, 1].\nn_iter=200: timed calls per size (at least 1), passed to benchmark_term.\ndirected=true: build directed (true) or undirected (false) networks.\nvertex_attributes=Dict{Symbol,Any}(): attr => spec pairs set on every generated network; spec is a function of the vertex index or a vector with one value per vertex (length ≥ the largest size).\nrng=Random.default_rng(): the source of every random draw.\n\nExample\n\nusing ERGM, ERGMUserterms, Random\nprof = profile_term(ExampleTerm(); sizes=[5, 10], n_iter=5, rng=Xoshiro(1))\n[r.n_vertices for r in prof]      # [5, 10]\n# An attribute-declaring term is timed on networks that carry its attributes\nprof = profile_term(InteractionTerm(:a, :b); sizes=[5, 10], n_iter=5, rng=Xoshiro(1),\n                    vertex_attributes=Dict(:a => (v -> Float64(v)), :b => (v -> 1.0)))\nlength(prof)                      # 2\n# ... and an undirected-only term on undirected networks\nprofile_term(TemplateTerm(1.0); sizes=[6], n_iter=5, directed=false, rng=Xoshiro(1))[1].ne <= 15   # true\n\n\n\n\n\n","title":"ERGMUserterms.profile_term"},{"category":"macro","location":"api/validation/#ERGMUserterms.@ergm_term","page":"Validation","text":"@ergm_term name body\n\nMacro for defining custom ERGM terms. After evaluating body, it verifies that the named type exists and that compute, change_stat, and name methods are defined for it, warning about anything missing. (Numeric consistency is checked separately with validate_term / change_stat_check, which need a term instance and a network.)\n\nThe generics must be imported by name before the macro is used (import ERGM: name, compute, change_stat); with a bare using, the body defines local functions of those names and the macro reports them as missing.\n\nExample\n\nusing ERGM, ERGMUserterms, Networks, Random\nimport ERGM: name, compute, change_stat\n\n@ergm_term ScaledEdges begin\n    struct ScaledEdges <: AbstractUserTerm\n        scale::Float64\n    end\n\n    name(t::ScaledEdges) = \"scalededges.$(t.scale)\"\n\n    compute(t::ScaledEdges, net) = t.scale * ne(net)\n\n    # Add-direction: one more edge adds `scale`, whatever the dyad's state\n    change_stat(t::ScaledEdges, net, i::Int, j::Int) = t.scale\nend\n\nnet = network(6; directed=true)\nadd_edge!(net, 1, 2); add_edge!(net, 2, 3)\ncompute(ScaledEdges(2.0), net)                                          # 4.0\nchange_stat(ScaledEdges(2.0), net, 1, 2)                                # 2.0\nvalidate_term(ScaledEdges(2.0), net; verbose=false, rng=Xoshiro(1))     # true\n\n\n\n\n\n","title":"ERGMUserterms.@ergm_term"},{"category":"section","location":"#ERGMUserterms.jl","page":"Home","text":"Develop a network statistic for ERGM.jl and verify that its change statistic agrees with full recomputation. ERGMUserterms.jl provides a Julia term interface, validation tools, benchmarks, and a copyable package template for extending binary ERGMs.\n\nFirst analysis Learn the model or data Reference and detail\nImplement a worked term Validate the implementation Browse the term interface\n\nnote: Supported scope\nThe validator checks computational consistency on the networks it exercises. It does not establish identifiability, model fit, or scientific validity. A custom term must declare its dependence and attribute requirements accurately; missing-data support is opt-in.","title":"ERGMUserterms.jl"},{"category":"section","location":"#Installation","page":"Home","text":"<p>Use Julia <strong>1.12 or newer</strong> and the <a href=\"/getting-started/\">shared workspace installation guide</a>. These development packages are not yet registered; the guide prepares the required sibling checkouts and a Julia environment for the examples.</p>","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"Home","text":"Implement a scaled edge count. Import the shared function names before adding methods:\n\nusing Networks, ERGM, ERGMUserterms, Random\nimport ERGM: name, compute, change_stat\n\nstruct ScaledEdgesPreview <: AbstractUserTerm\n    scale::Float64\nend\nname(t::ScaledEdgesPreview) = \"scaled_edges.$(t.scale)\"\ncompute(t::ScaledEdgesPreview, net::Network) = t.scale * ne(net)\nchange_stat(t::ScaledEdgesPreview, net::Network, i::Int, j::Int) = t.scale\nERGM.is_dyad_dependent(::ScaledEdgesPreview) = false\n\nnet = load_dataset(:florentine_marriage)\n@assert validate_term(ScaledEdgesPreview(2.0), net; rng=Xoshiro(1))\n\nchange_stat always returns the addition-direction difference, including when the edge already exists; the sampler handles the removal sign. This statistic is proportional to Edges(), so using both in one model would create redundant parameters.","title":"Quick Start"},{"category":"section","location":"#Not-implemented-(vs-R-ergm.userterms)","page":"Home","text":"What is ported is the role of ergm.userterms — a validated, copyable starting point for third-party terms — not its contents. R's package is a C template: its changestats.users.c skeleton and its worked example term mindegree are not ported, and nothing shipped here has them as a counterpart. A Julia term is plain Julia (three methods plus trait declarations), so there is no C skeleton to fill in; the Julia counterpart of the C template is the package template examples/MyTermPackage/, and the bundled example terms are instead pinned against the ergm terms they are identical to (see Validation against R).","title":"Not implemented (vs R ergm.userterms)"},{"category":"section","location":"#Term-Development-Workflow","page":"Home","text":"Step Function Description\n1. Define struct MyTerm <: AbstractUserTerm Create term struct with fields\n2. Implement name(), compute(), change_stat() Implement the three required methods\n3. Validate validate_term Check all methods work correctly\n4. Test test_term Run comprehensive tests on random networks (directed=false / vertex_attributes= for terms that need them)\n5. Benchmark benchmark_term Time compute vs change_stat\n6. Use Pass to ERGM.jl Put it in the term list of fit_ergm/ergm (ergm === fit_ergm, a const alias)","title":"Term Development Workflow"},{"category":"section","location":"#Documentation","page":"Home","text":"Pages = [\n    \"getting_started.md\",\n    \"guide/term_interface.md\",\n    \"guide/templates.md\",\n    \"guide/validation.md\",\n    \"guide/benchmarking.md\",\n    \"api/types.md\",\n    \"api/validation.md\",\n    \"api/utilities.md\",\n]\nDepth = 2","title":"Documentation"},{"category":"section","location":"#Theoretical-Background","page":"Home","text":"","title":"Theoretical Background"},{"category":"section","location":"#The-ERGM-Term-Interface","page":"Home","text":"In an Exponential Random Graph Model, the probability of a network mathbfY is:\n\nP(mathbfY = mathbfy) = frac1kappa(boldsymboltheta) expleft(sum_k theta_k g_k(mathbfy)right)\n\nWhere:\n\ng_k(mathbfy)\nare network statistics (terms) computed by compute()\ntheta_k\nare parameters to be estimated\nkappa(boldsymboltheta)\nis the normalizing constant","title":"The ERGM Term Interface"},{"category":"section","location":"#Change-Statistics-in-MCMC","page":"Home","text":"ERGM estimation relies on MCMC simulation, where edges are toggled one at a time. The change statistic for dyad (ij) is the add-direction difference\n\nDelta g_k(ij) = g_k(mathbfy^+_ij) - g_k(mathbfy^-_ij)\n\nwith mathbfy^+_ij/mathbfy^-_ij the network with the edge forced present/absent. change_stat() must return exactly this quantity — independent of the dyad's current state (the sampler negates it for removals). ERGMUserterms.jl validates both the value and its state-independence.","title":"Change Statistics in MCMC"},{"category":"section","location":"#References","page":"Home","text":"Hunter, D.R., Handcock, M.S., Butts, C.T., Goodreau, S.M., Morris, M. (2008). ergm: A package to fit, simulate and diagnose exponential-family models for networks. Journal of Statistical Software, 24(3), 1-29.\nMorris, M., Handcock, M.S., Hunter, D.R. (2008). Specification of exponential-family random graph models: Terms and computational aspects. Journal of Statistical Software, 24(4), 1-24.\nHunter, D.R. (2007). Curved exponential family models for social networks. Social Networks, 29(2), 216-230.\nRobins, G., Pattison, P., Kalish, Y., Lusher, D. (2007). An introduction to exponential random graph (p) models for social networks. *Social Networks, 29(2), 173-191.","title":"References"},{"category":"section","location":"#Citation","page":"Home","text":"If you use ERGMUserterms.jl in your work, please cite it using the entry in CITATION.bib:\n\n@misc{SNWJERGMUsertermsJL,\n  author = {{Statistical Network Analysis with Julia}},\n  title = {ERGMUserterms.jl: Custom ERGM Term Development for Julia},\n  year = {2026},\n  url = {https://github.com/statistical-network-analysis-with-Julia/ERGMUserterms.jl},\n  note = {Homepage: https://statistical-network-analysis-with-Julia.github.io/ERGMUserterms.jl; GitHub: https://github.com/statistical-network-analysis-with-Julia}\n}","title":"Citation"}]
}
